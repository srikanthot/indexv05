# Chatbot Scenario Catalog — coverage against the current index

Generated by a 12-agent analysis. Each scenario is scored **yes / partial / no** for whether the *current* index fields + retrieval can produce a complete, correct, cited answer. `partial`/`no` rows include the concrete gap + fix.

> Legend: ✅ yes · 🟡 partial · ❌ no

## Coverage summary

| Category | Scenarios | ✅ yes | 🟡 partial | ❌ no |
|---|---:|---:|---:|---:|
| procedures | 45 | 1 | 21 | 23 |
| table-values | 46 | 4 | 32 | 10 |
| figure-values | 44 | 7 | 36 | 1 |
| text-facts | 45 | 0 | 37 | 8 |
| summaries | 50 | 10 | 30 | 10 |
| highlighting-citation | 46 | 22 | 16 | 8 |
| safety-hazard-ppe | 45 | 3 | 41 | 1 |
| revision-currency | 45 | 8 | 27 | 10 |
| applicability-routing | 46 | 2 | 26 | 18 |
| cross-reference | 45 | 5 | 26 | 14 |
| comparison-troubleshoot | 47 | 1 | 43 | 3 |
| meta-navigation-nlq | 47 | 4 | 38 | 5 |
| **TOTAL** | **551** | **67** | **373** | **111** |


## procedures

| # | Question | | Gap & fix (if not yes) |
|---:|---|:--:|---|
| 1 | Give me the complete procedure for installing personal protective grounds on a de-energized 13kV distribution line, all steps. | 🟡 | Fields support it (procedure_id + procedure_step_order + procedure_step_count) but only via an app-side second-pass $filter query; a single vector top-k will drop mid-procedure chunks whose text doesn't match the query. Fix: orchestrator must, on any procedure hit, auto-expand `$filter procedure_id eq '…' & $orderby procedure_step_order` before answering; add a retrieval eval that fails if returned step numbers != procedure_step_count. |
| 2 | Walk me through the grounding procedure step by step and don't skip anything in the middle. | 🟡 | procedure_step_count gives the total, but it's computed by regex-counting numbered lines in section_content and is chunk-level identical, not a per-step manifest. If a SplitSkill boundary cut a step or a step lacked a number, the count is silently wrong. Fix: emit a step_numbers_present array per procedure so the bot can diff actual vs expected and warn 'chunk missing' instead of trusting a bare integer. |
| 3 | What are the steps between step 4 and step 9 of the regulator bypass procedure? | ❌ | procedure_step_order is the chunk's FIRST step only; individual steps are concatenated inside procedure_step_text as one blob, not filterable per step. A range query `order ge 5 and le 8` returns whole chunks, not steps 5-8. Fix: index each step as its own table_row-style record (step_number Int, step_text, page) so ranges, 'step N', and 'between X and Y' become exact filters. |
| 4 | What's the very next step after step 7 in the capacitor bank switching procedure? | ❌ | No per-step addressability — step 7 and 8 may live in the same concatenated procedure_step_text chunk or straddle a chunk boundary with no step-level index. chunk_next_id is an empty stub. Fix: per-step records with step_number + a next_step_number, or at minimum populate chunk_next_id so continuation is deterministic. |
| 5 | Continue — show me the rest of that procedure from where it left off. | 🟡 | chunk_prev_id/chunk_next_id are documented-empty stubs, so 'continue' can't traverse the linked list. It works ONLY within a procedure via procedure_id + next-higher procedure_step_order. Fix: either populate the prev/next stubs at index time, or have the orchestrator track last procedure_step_order and re-query the same procedure_id for the next chunk. |
| 6 | Continue reading this section (a non-procedure safety narrative) onto the next page. | ❌ | Only procedures have an ordering key. For prose/narrative chunks there is no next-chunk link (prev/next empty) — layout_ordinal orders within a single page but not across pages, and there's no cross-page reading-order sequence field. Fix: compute a document-global reading_order Int (or populate chunk_next_id) so 'continue' works for any content, not just numbered procedures. |
| 7 | Did I miss a step? I did 1, 2, 3, then 5, 6 of the mark-out procedure. | 🟡 | procedure_step_count would flag that 6 != total, but to name 'you skipped step 4' the bot needs the actual text of step 4, which is buried in a concatenated procedure_step_text blob rather than individually retrievable. Fix: per-step records let the bot fetch step 4 verbatim and cite its page; today it can only say a count mismatch exists. |
| 8 | Give me the lockout/tagout / clearance sequence for a 4kV feeder before I open the disconnect. | 🟡 | If the LOTO is a numbered list it groups fine, but there is no record_type or classifier distinguishing a LOTO/clearance sequence from a generic procedure — retrieval leans on title text matching. And LOTO warnings on continuation chunks may be truncated (governing_callouts keeps only the first line of a multi-line boxed WARNING). Fix: add a procedure_kind facet (loto\|switching\|grounding\|purging) and stop truncating multi-line callouts. |
| 9 | Show the feeder switching order to transfer load from Bus 1 to Bus 2. | ❌ | Switching orders are typically TABLES; process_table.py hardcodes procedure_id='' and procedure_step_order=None, so table-based sequences get NO procedure grouping or ordering. The bot sees disconnected table_row records. Fix: run procedure parsing over ordered table rows (use table_row_index as step_order) and assign a shared procedure_id to switching-order tables. |
| 10 | Give me the restoration/re-energizing sequence — basically the switching order in reverse. | ❌ | No reverse-order or paired isolation/restoration linkage exists. If the manual prints a separate restoration procedure it can be retrieved as its own numbered list; if it only says 'reverse the above steps', nothing links restore to isolate. Fix: add a linked_reverse_procedure_id / sequence_direction field so 'restore' maps to the correct paired sequence. |
| 11 | What are the prerequisites before I start the live-line hot-stick cover-up procedure? | ❌ | There is no prerequisite/precondition field. Prereqs ('line must be tested dead', 'rubber goods inspected') live as prose above step 1 and are only captured if they happen to be a chunk of the same procedure_id — not distinguishable from steps. Fix: add a preconditions field extracted from 'before you begin / prerequisites / required' blocks so the bot can answer prereqs without dumping the whole procedure. |
| 12 | In the gas main purging procedure, if the pressure reads above 2 psi, which step do I go to? | ❌ | procedure_branch_label only captures the first 'if/when/unless…' phrase in a chunk, truncated to 80 chars, with NO target step and only one per chunk. It cannot answer 'go to step X'. Fix: extract branches as {condition, then_step_order} tuples per step so conditional routing is answerable and citable. |
| 13 | Show me both branches: what to do if the valve holds pressure vs. if it doesn't. | ❌ | Only a single procedure_branch_label is stored per chunk (first regex match), so a step with two outcomes loses one. Fix: make procedure_branch_label a Collection of structured branch objects rather than one truncated string. |
| 14 | Break out the sub-steps of step 6 (6a, 6b, 6c) of the transformer tap-change procedure. | ❌ | _STEP_RE only matches top-level integers with a trailing space; lettered sub-steps ('a.') and decimal sub-steps ('6.1 ' fails the \s+ after the first dot) are never detected, so sub-structure is flattened into procedure_step_text or lost. Fix: extend the parser to capture sub-step markers with a parent_step_order and index them hierarchically. |
| 15 | The manual describes the procedure in prose — 'First de-energize, then test, then ground.' Give me those steps in order. | ❌ | looks_like_procedure requires >=2 NUMBERED, mostly-increasing steps. Prose/bulleted procedures get procedure_id='' and are never grouped or ordered — a large fraction of safety narrative is prose. Fix: add prose-sequence detection (ordinal discourse markers, imperative bullet lists) to assign procedure_id/order, or an LLM step-segmentation pass. |
| 16 | How many total steps are in the underground transformer inspection procedure? | 🟡 | procedure_step_count exists and is answerable, but it's a raw regex count of numbered lines in section_content — it over/under-counts when numbering restarts, a sub-list reuses '1.', or a table interrupts the list. Fix: validate the count against a de-duplicated max(step_order) and reconcile restarts before trusting it as 'total steps'. |
| 17 | Two different procedures are under the same heading 'Field Operations'. Give me just the second one. | ❌ | procedure_id = hash(parent + deepest header), so two procedures under one heading COLLIDE into a single procedure_id — their steps interleave and step_count doubles. Fix: seed procedure_id with an additional discriminator (first-step anchor, layout_ordinal band, or an LLM-detected procedure boundary). |
| 18 | Give me the current-revision version of the grounding procedure, not the superseded one. | 🟡 | is_current_revision is set only by mark_current_revisions.py (dry-run default, not in heal flow), so it's frequently null; a bot filtering is_current_revision eq true fails-closed and returns an EMPTY procedure. Fix: make current-revision marking part of the indexing/heal path so every record has a real boolean; until then, treat null as current. |
| 19 | Which page is step 12 of the network protector maintenance procedure on? | 🟡 | Pages are chunk-level (physical_pdf_page, printed_page_label, page_end). If step 12 sits in a chunk covering steps 9-14 across two pages, the bot can only cite the chunk's page span, not step 12's exact page. Fix: per-step records carry their own page; or store a step_order→page map within the chunk. |
| 20 | Show me the figure that goes with step 3 of the elbow-connector procedure. | 🟡 | figure_step_linked/figure_linkage_confidence are real ONLY on diagram records and hardcoded False/0.0 on text/table/summary, and the link is figure↔chunk, not figure↔specific step number. figures_referenced_normalized on the text chunk helps find the figure but not 'the one for step 3'. Fix: capture step_order on the figure linkage so a step can name its figure. |
| 21 | List every WARNING and DANGER that applies to the live-line jumper installation procedure. | 🟡 | governing_callouts is computed section-wide (good for recall) BUT multi-line boxed callouts are truncated to their first line, so multi-sentence WARNINGs lose content; and safety_callout is over-flagged (NOTE/NOTICE treated as callouts). Fix: stop first-line truncation, store full callout text, and separate advisory NOTE from mandatory WARNING/DANGER. |
| 22 | For the switching order in that table, what warnings are attached to each operation? | ❌ | table_row records set safety_callout but NEVER emit the callouts keyword field, so callout-filtered retrieval misses every table row — and switching orders are tabular. Fix: emit callouts on table_row records and bind row-level warnings. |
| 23 | Resume the purging procedure at step 5 and give me 5 through the end. | 🟡 | Chunk-level ordering lets the bot fetch chunks whose procedure_step_order >= 5, but a chunk starting at step 3 that also contains step 5 won't be caught by an order>=5 filter, and step 5 can't be isolated from the concatenated blob. Fix: per-step records make 'from step 5 onward' an exact ordered slice. |
| 24 | I'm at step 8 — what should I have already completed and verified before this point? | 🟡 | Can return earlier chunks by procedure_id + lower order, but 'hold points' / 'verify before proceeding' gates aren't a distinct field — they're plain text inside steps. Fix: extract hold_point/verify markers as a field so the bot can call out gating checkpoints, not just prior prose. |
| 25 | The procedure spans pages 14-17 across two chunks that overlap — don't show step 9 twice. | 🟡 | procedure_step_id = procedure_id + first-step anchors a chunk's slice for dedupe, but two overlapping windows can share the same first step yet differ in tail steps, or differ in first step while both containing step 9 — so blob-level dedupe still double-prints interior steps. Fix: per-step records with a unique (procedure_id, step_order) key make dedupe exact. |
| 26 | A continuation chunk has only a WARNING box and no step numbers — is it part of the procedure, and where does it belong in order? | 🟡 | Continuation/warning chunks get procedure_step_order = None by design (kept in the group via procedure_id). But sorting by a nullable order puts them first-or-last unpredictably, so the WARNING can render detached from the steps it governs. Fix: assign continuation chunks an interpolated order (e.g. layout_ordinal-derived) so they sort into their true position. |
| 27 | Give me the equipment-specific variant: the grounding steps for a padmount vs. an overhead installation. | ❌ | applies_to_system merely echoes header_1/header_2 (not a real controlled vocabulary), and applies_to_equipment is free text, so variant filtering ('padmount' vs 'overhead') is unreliable and won't cleanly separate two procedure variants. Fix: populate applies_to_* from a controlled vocab and tag procedure variants explicitly. |
| 28 | What's step 1 of the phasing procedure, exactly as written — read it verbatim. | 🟡 | The chunk containing step 1 exists and procedure_step_text holds it, but step 1 is concatenated with steps 2..n in one field; the bot must string-parse to isolate verbatim step 1 (parser normalizes whitespace, so 'exactly as written' spacing/line breaks are already lost). Fix: store per-step verbatim text preserving original formatting. |
| 29 | Cross-reference: step 10 says 'return to step 4' — show me step 4. | ❌ | sections_referenced captures section/figure/table refs, not step-number references, and there's no per-step addressing to jump to. The 'return to step 4' is just text. Fix: parse intra-procedure step references into a structured link and enable step addressing. |
| 30 | Is this a complete procedure or did the document cut it off? Steps end at 11 but it feels unfinished. | 🟡 | procedure_step_count reflects steps parsed from section_content, so if the source truly ends at 11, count=11 and the bot can't tell 'the manual stops here' from 'a chunk didn't index'. No source-completeness flag. Fix: add a step-contiguity/section-boundary flag distinguishing source truncation from indexing gaps. |
| 31 | Turn the whole clearance procedure into a checklist I can tick off in the field. | 🟡 | Works only if the procedure is fully assembled and steps are individually enumerable; today's concatenated per-chunk step_text and possible interior gaps make a reliable 1..N checklist fragile. Fix: per-step records give a clean enumerable list for checklist rendering with per-step page citations. |
| 32 | Combine the de-energize, test-dead, and ground procedures into one sequence in the right order. | ❌ | There is no inter-procedure ordering — each procedure_id is independent with no 'this procedure precedes that one' link. The bot can't know de-energize comes before grounding except from prose. Fix: add a procedure sequence/dependency field (precedes/follows procedure_id). |
| 33 | Which steps require a second qualified worker or a standby person? | ❌ | No structured requirement field for staffing/qualification per step; 'two-person' notes are plain text inside procedure_step_text and only findable by keyword search, not filterable. Fix: extract per-step requirement tags (roles/PPE/tools) so 'which steps need X' is a filter. |
| 34 | At step 6 it says 'if energized, STOP' — surface that stop/abort condition prominently. | ❌ | An abort condition is captured, if at all, only as a generic procedure_branch_label with no severity and possibly truncated at 80 chars; it isn't flagged as stop-work. Fix: classify branches (continue\|goto\|abort/stop) and tie abort branches to hazard_class/criticality for prominent surfacing. |
| 35 | Renumber and give me the steps for a procedure whose numbering restarts at 1 halfway through (two sub-lists). | ❌ | looks_like_procedure tolerates one non-increase, and parse_steps takes raw integers, so a restart to '1' corrupts ordering and inflates procedure_step_count (duplicate low numbers). procedure_step_order = min = the restarted 1. Fix: normalize restarts (detect sub-list boundaries, assign hierarchical or continuous ordering). |
| 36 | Show only the mandatory steps, skip the informational NOTEs. | ❌ | safety_callout/criticality over-flag: the callout regex is case-insensitive and treats NOTE/NOTICE as callouts, so mandatory vs advisory isn't cleanly separable, and NOTEs interleaved in a numbered list may be parsed as steps. Fix: classify callout severity (advisory vs mandatory) and exclude NOTE lines from step parsing. |
| 37 | For the regulator-pit entry procedure, what must be true about atmospheric testing before step 1? | ❌ | No preconditions field; confined-space/atmospheric-test gates are prose above step 1 and only present if that chunk shares the procedure_id. Fix: extract 'before you begin' preconditions into a dedicated, filterable field. |
| 38 | Give me the steps and, for each, the tool or PPE it calls for. | ❌ | Tools/PPE mentioned in steps are unstructured text inside procedure_step_text; no per-step tool field. Fix: extract per-step tools/PPE so the bot can build a step→equipment table. |
| 39 | How long is this procedure — will it fit on one screen or is it a 20-step job? | ✅ |  |
| 40 | Show me just the last step of the de-energization procedure — how does it end? | 🟡 | Can locate the last chunk via highest procedure_step_order, but the final step is inside a concatenated blob and there's no explicit 'procedure complete / release' flag distinct from step text. Fix: per-step records plus a completion/step-final marker. |
| 41 | Compare this revision's grounding procedure to the superseded one — what steps changed? | ❌ | supersedes_revision is often null (not emitted by skillset, set only by a dry-run script), and procedure_id is per-document so the same procedure across two revisions gets DIFFERENT ids — no cross-revision step alignment. Fix: add a revision-independent procedure_family_id and reliably populate supersedes_revision to enable step-level diffs. |
| 42 | I only remember it's the procedure with the WARNING about induced voltage — pull up that whole procedure. | 🟡 | governing_callouts is searchable so the warning chunk is findable, and it carries procedure_id for expansion — but if the warning was truncated to its first line the 'induced voltage' phrase may be lost, and if the warning sits on a table row it lacks callouts entirely. Fix: full callout text + callouts on table rows. |
| 43 | Read me the switching order operations in exact device-operation order with each device ID. | ❌ | Switching-order tables have procedure_step_order=None and no procedure_id; ordering must fall back to table_row_index, which the retrieval layer doesn't treat as a procedure sequence. Device IDs live in table_row_cells but aren't bound into an ordered procedure. Fix: promote ordered table rows to a procedure with table_row_index as step_order. |
| 44 | Does this procedure have any branches at all, or is it strictly linear? | 🟡 | procedure_branch_label being non-empty on any chunk hints a branch exists, but it's a fuzzy first-match phrase (also fires on benign 'when complete…'), so presence detection is noisy and per-chunk only. Fix: a structured has_branch boolean plus enumerated branch objects gives a reliable answer. |
| 45 | Give me the full mark-out procedure and confirm you've shown all N steps with page citations for each. | 🟡 | End-to-end this stacks every gap: full assembly needs app-side expansion, count accuracy is regex-limited, and citations are chunk-level not per-step, so 'page for each step' can't be honored precisely. Fix: per-step records (step_order, text, page) + a validated total + orchestrator auto-expansion together make this fully answerable and auditable. |

## table-values

| # | Question | | Gap & fix (if not yes) |
|---:|---|:--:|---|
| 1 | What's the minimum approach distance for a qualified worker on a 138 kV phase-to-ground exposure? | 🟡 | The 138 kV row is retrievable via table_row_search_text/semantic_key, but table_row_cells is a flat Collection(Edm.String) with no per-cell column-header binding, so the LLM must guess which cell is phase-to-ground vs phase-to-phase. Add a structured cell map (table_row_cells_by_column: column_header->value) so the exact column can be resolved deterministically. |
| 2 | MAD for 500 kV — give me both phase-to-ground and phase-to-phase. | 🟡 | table_columns lists headers and table_row_cells lists values but the two are positionally correlated only; merged/spanning header rows (table_header_rows_count>1) break alignment. Emit an explicit ordered column-to-cell pairing per row so multi-column answers are unambiguous. |
| 3 | We're working at 4,200 ft elevation — does the 230 kV approach distance need an altitude correction? | ❌ | footnotes is a page-level Collection with no linkage to the specific row or cell that carries the marker, so the altitude-correction note is not reliably attached to the 230 kV MAD value. Add table_row_footnote_refs that map footnote markers in a cell to footnote text, and surface the correction factor with the value. |
| 4 | What is the maximum use voltage for a Class 2 rubber insulating glove? | 🟡 | Retrievable by table_row_semantic_key='Class 2', but the value is stored as a bare string; there is no normalized numeric/unit field so the bot cannot validate '17,000 V' vs a job at 17.5 kV. Add value_numeric + value_unit for rating cells to enable safe comparison. |
| 5 | Which glove class do I need for a 26.5 kV circuit? | ❌ | No numeric fields on rows means the bot cannot evaluate 'rating >= 26.5 kV' across rows; it can only string-match. Add per-row value_numeric and enable range filtering / server-side numeric compare so reverse-lookups are correct rather than approximate. |
| 6 | What torque should I use for a 5/8-inch connector bolt on a copper compression lug? | 🟡 | The torque table row is retrievable, but selecting by BOTH size and material relies on both appearing in table_row_search_text; there is no per-row structured key set. Multi-key rows need composite keys (table_row_semantic_key currently single) or the column-map fix so 5/8-in + copper resolves to one cell. |
| 7 | Ampacity of 4/0 ACSR at a 75 °C conductor temperature? | 🟡 | Classic matrix lookup: row found via semantic_key, but the 75 °C column must be picked out of table_row_cells with no header binding, and the °C unit lives only in the header. Add column-map + unit inheritance from header so the correct ampacity cell is returned with its condition. |
| 8 | What does the third row of Table 5-2 actually say? | ✅ |  |
| 9 | Table 4-1 continues onto the next page — what are the ratings in the rows after the page break? | 🟡 | table_cluster_id + table_split_index/count let you gather continuation rows, but retrieval returns only the top-scoring row/split; the bot must issue a secondary filter query on table_cluster_id to pull the rest. Add orchestration guidance (or a retrieval-time expansion) so continuation rows are always reassembled, and confirm the header row is propagated to later splits. |
| 10 | Convert that 4 ft 6 in clearance to meters for me. | ❌ | Values are stored as display strings like "4'-6\"" with no parsed numeric, so conversion depends entirely on the LLM parsing an ambiguous feet-inches string. Add value_numeric in canonical units (e.g., inches) so conversions are deterministic and auditable. |
| 11 | Minimum depth of cover for a plastic gas main under a public street? | 🟡 | Row retrievable, but 'under a public street' vs 'private property' are separate columns; without column-map the bot may return the wrong depth. Column-to-cell binding fix needed; also confirm applies_to_domain='gas' is set at row level, not just chunk level. |
| 12 | What's the MAOP for a Class 3 location on this steel main? | 🟡 | Value retrievable but psig unit and the location-class condition are column/header-bound; no numeric field for downstream 'is our 240 psig within MAOP' checks. Add value_numeric+unit and column binding. |
| 13 | What size grounding/jumper cable is required for a 20 kA available fault current? | ❌ | Requires numeric band comparison (e.g., 'up to 21 kA -> 2/0'); with only string cells the bot cannot reliably match 20 kA to the right band. Add numeric min/max band fields per row to support banded lookups safely. |
| 14 | Continuous current rating of a 100 A cutout — is the row a header or real data? | 🟡 | table_row_is_header_like/is_index_like/is_placeholder_like flags exist, but retrieval does not automatically down-rank header-like rows, so a header row can surface as the 'answer.' Add a filter (retrieval_eligible excludes is_header_like) or scoring penalty so only true data rows answer value questions. |
| 15 | Minimum live-line tool (hot stick) length for 115 kV work? | 🟡 | Row retrievable; voltage matching relies on text since applies_to_voltage is chunk-level (whole table) not per-row. Add row-level applies_to_voltage so the 115 kV row is filterable, and numeric length for validation. |
| 16 | What class of insulating blanket/cover-up is required at 34.5 kV? | 🟡 | Same reverse-lookup limitation: string cells only. Row exists but matching 34.5 kV to the covering class band needs numeric bands. Add band fields; also link to any '*use next higher class' footnote via row->footnote refs. |
| 17 | Required working clearance in front of 480 V switchgear per the dimensions table? | 🟡 | NEC-style tables have Condition 1/2/3 columns; column-map absence means the bot may read the wrong condition's clearance. Add column binding and per-cell unit. |
| 18 | How far must a crane boom stay from an energized 69 kV line? | 🟡 | Retrievable but same voltage-row matching + numeric gap; also the correct table (equipment clearance vs worker MAD) must be disambiguated — add table_scope_tags usage guidance so equipment-clearance vs approach-distance tables aren't confused. |
| 19 | Which voltages in Table 2 require more than 3 feet of clearance? | ❌ | No numeric cell field, so a 'value > 3 ft' filter across rows is impossible; the bot would have to retrieve every row and parse strings, unreliable when rows are truncated. Add value_numeric to make threshold queries answerable and complete. |
| 20 | Give me the fault-current withstand rating versus duration for this device. | 🟡 | Each duration is a separate column; flat cells + header ambiguity make pairing error-prone. Column-map fix plus ensuring all columns survive (table_col_count matches cell count) needed. |
| 21 | What ambient-temperature derating factor applies to conductor ampacity at 40 °C? | 🟡 | Derating factor cell retrievable but the temperature condition is header-bound and the factor is unitless (easy to misread as amps). Add column binding + a value_type tag (factor vs absolute) so the bot applies it correctly. |
| 22 | The primary conductor sag table — what's the sag for 795 ACSR at 120 °F, 0 wind? | 🟡 | Two-dimensional conditioning collapses into a single flat row; without column binding the temp/wind column can't be reliably selected. Structured column map required; confirm the table wasn't split mid-condition-block (table_split_index). |
| 23 | Show me the full Table 3-4 including the note markers on each value. | ❌ | footnotes are page-level and in-cell markers are not preserved as structured refs, so a faithful reproduction loses which value carries which note. Preserve in-cell markers and add row/cell->footnote linkage. |
| 24 | What does the row for 'Type K' fuse link say versus 'Type T'? | ✅ |  |
| 25 | Is the value in this table the current revision, or was it superseded? | 🟡 | is_current_revision/supersedes_revision/effective_date exist at doc level, so the bot can state currency IF retrieval didn't pull a superseded doc. Ensure retrieval filters is_current_revision=true by default; otherwise a stale table value could be cited as current. |
| 26 | That torque number looks like it might be an OCR error — how confident is it? | 🟡 | low_confidence_ocr and table_row_min_confidence exist at row level, but confidence isn't per-cell, so a single misread digit in one cell isn't isolated. Add per-cell confidence, and have the bot explicitly warn + cite page image when table_row_min_confidence is low on a rating. |
| 27 | Some rows seem missing from this ratings table — did anything get dropped? | 🟡 | table_rows_truncated and table_rows_suppressed_count record that rows were dropped, but the retrieval layer does not force disclosure; a user could get a confident-but-incomplete answer. Add logic: when the answer table has rows_truncated=true or suppressed>0, the bot must warn and point to the source page rather than imply completeness. |
| 28 | The exact conductor size I need was in a low-quality row — can you still give it? | ❌ | Rows below quality threshold may be excluded from retrieval_eligible entirely, so a genuinely needed value becomes unretrievable. Fix: keep low-quality rows indexed but flagged (table_row_quality) and allow a fallback pass that returns them with an explicit reliability warning + page image. |
| 29 | Pressure-test hold time and test pressure for a new 2-inch plastic service? | 🟡 | Retrievable but pressure vs duration columns need binding, and units (psig vs minutes/hours) live in headers. Column-map + unit inheritance fix. |
| 30 | Guy/anchor rod size for a 12,000 lb load? | ❌ | Load bands are string cells; 12,000 lb can't be matched to a band numerically. Add numeric min/max load fields per row. |
| 31 | Maximum overcurrent-protection rating (% of FLA) for this 3-phase transformer? | 🟡 | Percent value retrievable but the condition columns (supervised/unsupervised, primary/secondary) require binding; value_type (percent vs amps) also needed to avoid misinterpretation. |
| 32 | Approach distance for an UNqualified person near 12 kV — different from qualified? | 🟡 | Qualified vs unqualified are typically separate columns or tables; without column binding and table_scope_tags disambiguation the bot may return the qualified-worker value to an unqualified-person question — a safety error. Add column binding + scope-tag driven table selection. |
| 33 | Proof-test voltage and minimum length for a fiberglass hot stick rated 100 kV? | 🟡 | Retrievable; multi-column binding + units gap as above. |
| 34 | How many insulators / what BIL is needed for a 138 kV suspension string? | 🟡 | Row-level voltage filter missing (applies_to_voltage is chunk-level) and two columns need binding. Add per-row applies_to_voltage + column-map. |
| 35 | Required vertical separation where a gas main crosses an electric duct bank? | 🟡 | Retrievable but system-pair condition and unit are column-bound; add binding and numeric value. |
| 36 | Padmount transformer clearance dimensions — front, sides, rear? | 🟡 | Front/side/rear are distinct columns collapsed into flat cells; column-map required so each dimension is labeled correctly. |
| 37 | This manual has both a metric and an imperial version of the clearance table — which one am I reading? | 🟡 | table_variant_id and table_scope_tags exist to separate variants, but retrieval may interleave metric and imperial rows if the query doesn't specify units. Add variant-aware retrieval (prefer the variant matching the user's requested unit) so metric/imperial rows aren't mixed in one answer. |
| 38 | Step 6 says 'torque per Table 4' — what's the actual value? | 🟡 | tables_referenced_normalized on the procedure chunk names 'Table 4', and table_number lets you fetch it, but there's no automatic join — the bot must do a second query, and if Table 4 has multiple rows it still needs the right key from the step context. Add retrieval-time cross-ref expansion from procedure step to referenced table rows. |
| 39 | What's in the row whose first cell is 'Aluminum, bare' in the connector table? | ✅ |  |
| 40 | The header just says 'kV' but the cells are bare numbers — are these kilovolts? | 🟡 | Units frequently live only in table_columns headers while table_row_cells hold bare numbers; without column-map the unit isn't attached to the value. Column binding + unit inheritance closes this. |
| 41 | Compare the 15 kV and 25 kV rows — how much more clearance does 25 kV need? | 🟡 | Both rows retrievable, but the subtraction depends on parsing string values; feet-inches formats make this error-prone. Add value_numeric so cross-row arithmetic is exact. |
| 42 | Footnote 'b' next to the 345 kV value — what exception does it state? | ❌ | footnotes exist as a page-level Collection with no marker-to-text mapping and no cell linkage, so 'b' can't be resolved to the right note. Add structured footnote records (marker->text) and row/cell->footnote refs. |
| 43 | Does the 500 kV MAD assume phase-to-phase overvoltage factor 3.0 — what's the note say? | ❌ | The overvoltage-factor condition is a table note not linked to the row; same footnote-linkage gap. Without it the cited MAD could be applied under wrong assumptions — a safety risk. Add row->footnote linkage and expose the assumption alongside the value. |
| 44 | What are ALL the rows in the minimum-approach-distance table for AC systems, in order? | 🟡 | table_row_index allows ordering and table_cluster_id groups the table, but retrieval top-k may not return every row and suppressed/low-quality rows are omitted. Add a 'fetch full table by cluster_id' path plus disclosure when rows were suppressed/truncated, so enumeration is provably complete. |
| 45 | Give me the closest thing to a citation: which page and printed page is Table 6-1 on? | ✅ |  |
| 46 | What's the ampacity if the table row wraps across a column break and the value sits in the second split? | 🟡 | table_split_index/count and table_cluster_id handle splits, but if a wide table is split by COLUMNS the header-to-cell alignment and column-map (already missing) get worse. Ensure split metadata records split axis and that column headers are duplicated to each split; add column-map so post-split cells stay labeled. |

## figure-values

| # | Question | | Gap & fix (if not yes) |
|---:|---|:--:|---|
| 1 | Show me Figure 18-117. | ✅ |  |
| 2 | What is part 3 in Figure 18-117? | ❌ | figure_callouts is a flat deduped token list (e.g. '3', 'valve seat', 'M6 bolt') with NO pairing of number->part; diagram_ocr_text is one blob. Bot cannot reliably say 'part 3 = valve seat'. Fix: add a structured field figure_callout_map (Collection of 'N=label' pairs) parsed from the VLM JSON, make it searchable/filterable; until then answer must show the crop and decline to assert the pairing. |
| 3 | What voltage rating is on the transformer nameplate in Figure 5-2? | 🟡 | Nameplate values are transcribed into free-text diagram_description ('Voltage rating: 13.2kV...') and diagram_ocr_text, so retrieval finds the figure and surfaces the string, but there are no discrete nameplate_voltage fields and no per-figure OCR confidence. Per code policy VLM reads aren't asserted as fact. Fix: add structured nameplate_* fields + diagram_low_confidence flag; answer must show crop and caveat the value. |
| 4 | Which figure shows the recloser control wiring? | ✅ |  |
| 5 | In the one-line diagram on page 44, what is the CT ratio on the main breaker? | 🟡 | Can locate the figure (page filter + diagram_category='line_diagram') and surface diagram_ocr_text tokens like '600:5', but cannot reliably bind '600:5' to 'main breaker' — no structured connection/component map, no per-token bbox. VLM value not asserted per policy. Fix: show crop + list OCR tokens as unverified; longer-term add per-token bbox and component-value pairs. |
| 6 | Trace the wire from terminal J1 pin 3 in Figure 7-4 — where does it go? | 🟡 | The schematic prompt asks the VLM to narrate 'J1 pin 3 -> TB-2 terminal A' into diagram_description, so a connection may be described in prose, but there is no structured figure_connections field to trace reliably and no verification. Fix: add figure_connections Collection of from->to edges; meanwhile show crop and quote the described connection with a read-it-yourself caveat. |
| 7 | What are all the callout numbers in the exploded view of the regulator, Figure 12-9? | 🟡 | figure_callouts holds the tokens (numbers + parts) but is retrievable-only (not searchable/filterable), deduped and capped at 200, and loses declaration order. You get an unordered mix of numbers and labels. Fix: split into figure_callout_numbers (ordered) vs labels, make searchable; preserve order. |
| 8 | Show me the nameplate photo for the 500 kVA pad-mount transformer. | 🟡 | diagram_category='nameplate' is facetable and applies_to_equipment may carry 'transformer', but kVA rating ('500 kVA') is only in free-text description/OCR, not a filterable field, so precise matching relies on full-text/vector. Fix: add structured nameplate_power_rating; today rank by vector search over description and show the crop. |
| 9 | What does symbol with the triangle-and-line mean in the schematic Figure 3-1? | 🟡 | If a legend is present, the VLM may describe it in diagram_description, but there is no structured legend field mapping symbol glyph -> meaning, and glyphs aren't OCR-able as text. Fix: show the crop (esp. the legend region) and surface any described legend text; add a figure_legend field if legends are common in the corpus. |
| 10 | What is the setpoint shown for the pressure switch in the P&ID on page 88? | 🟡 | block_flow prompt captures loop tags (e.g. 'PSL-101') and 'setpoints or threshold values' into description; retrieval finds the figure and may surface the number, but tag->setpoint pairing isn't structured and value isn't asserted per policy. Fix: show crop + quote described setpoint as unverified; add structured instrument/setpoint pairs. |
| 11 | What torque spec is called out next to callout 6 in the exploded assembly, Figure 9-3? | 🟡 | parts_exploded prompt asks VLM to 'capture torque/fastener specs alongside callouts' into description, but there is no structured callout->spec field and figure_callouts doesn't pair them. Fix: show crop; add figure_callout_map with associated spec text; caveat any quoted torque value. |
| 12 | Which figures in Chapter 18 are wiring diagrams? | 🟡 | diagram_category is facetable but diagram records get chapter context only via header_1/2/3 (chapter_label/chapter_number are populated on TEXT records, not stamped on diagram records in diagram.py). Fix: propagate chapter_label/chapter_number onto diagram records; today filter by header_1 text + diagram_category which is fuzzy. |
| 13 | Read me the model and serial number off the breaker nameplate in Figure 22-5. | 🟡 | diagram_ocr_text transcribes model/serial verbatim and it's searchable, so the string is retrievable, but there's no per-field structure and no OCR confidence on diagram records to flag a garbled serial. Fix: add nameplate_model/nameplate_serial + diagram_low_confidence; answer shows crop and quotes OCR with 'verify against the plate' caveat. |
| 14 | What are the fuse ratings shown on the one-line diagram, Figure 4-2? | 🟡 | OCR tokens like '15A','65E' land in diagram_ocr_text (searchable) and figure_callouts, but aren't bound to the specific fuse position and values aren't asserted. Fix: show crop; list OCR'd rating tokens as unverified; add component-value pairing for one-lines. |
| 15 | Which figure is referenced in section 5.3.2 about grounding? | ✅ |  |
| 16 | Show the grounding/bonding detail figure and tell me the required clearance dimension. | 🟡 | Figure retrieval + crop works; dimension values (e.g. '6 in min') appear in diagram_ocr_text if legible but aren't asserted and have no confidence. Fix: show crop with the dimension region and quote OCR value as unverified; add per-figure OCR confidence to flag illegible dimensions. |
| 17 | What phase is each conductor in the three-phase riser diagram, Figure 6-1? | 🟡 | applies_to_phase tags are mined from surrounding text, and phase letters may be in diagram_ocr_text, but there's no structured conductor->phase mapping and no spatial anchoring. Fix: show crop; surface phase tokens; add structured labels if phase diagrams are common. |
| 18 | The value at callout 3 in Figure 18-117 looks blurry in my copy — what does the manual say it is? | 🟡 | diagram_ocr_text may contain the value but there is NO per-figure/per-token OCR confidence stamped on diagram records (ocr_min_confidence/low_confidence_ocr are only set for text/table records), so the bot can't tell the operator whether its own read is trustworthy. Fix: stamp diagram OCR confidence per token; answer must caveat and show the crop from the source PDF. |
| 19 | Which figure shows the interior of the switchgear cabinet? | ✅ |  |
| 20 | List every reference designator (R1, C2, K1...) in the control schematic Figure 11-8. | 🟡 | schematic prompt asks VLM to transcribe 'EVERY reference designator in declaration order' into description/OCR, and figure_callouts captures them, but figure_callouts is retrievable-only (not searchable) and order isn't guaranteed. diagram_ocr_text is searchable but a blob. Fix: dedicated searchable, ordered designator list field. |
| 21 | Compare the terminal layout in Figure 7-4 vs Figure 7-5. | 🟡 | Both figures retrievable with crops and descriptions, so a narrative compare + show-both is possible, but there's no structured terminal list per figure to do a precise diff and values aren't asserted. Fix: show both crops; structured connection/terminal fields would enable a real diff. |
| 22 | On the regulator diagram, what gas pressure is marked at the inlet? | 🟡 | Domain=gas figures retrievable; pressure token may be in diagram_ocr_text but not bound to 'inlet' and not asserted. Fix: show crop; quote OCR token as unverified; spatial token bboxes would let the UI highlight the inlet value. |
| 23 | What is Figure 18-117 titled / what is its caption? | ✅ |  |
| 24 | How many figures are in the transformer maintenance chapter? | 🟡 | Can count diagram records but scoping to a chapter is weak because chapter_label/chapter_number aren't stamped on diagram records (only header_1/2/3). Multi-crop/decorative figures also inflate counts. Fix: propagate chapter fields to diagrams and exclude has_diagram=false/decorative from the count. |
| 25 | Show the figure on printed page 12-7. | 🟡 | Diagram printed_page_label is SYNTHETIC (str(physical_pdf_page)) per diagram.py:508 — it's the physical PDF page number, not the manual's printed label like '12-7'. So filtering by the printed label '12-7' misses figures. Fix: resolve real printed label for figures (currently only text records run extract-page-label). |
| 26 | This callout in Figure 9-3 points to a part — what's the part number so I can order it? | 🟡 | Part numbers land in diagram_ocr_text/figure_callouts but aren't paired to the callout number, and there's no link from the figure callout to the parts-list TABLE row that defines it. Fix: add figure_callout -> table_row_semantic_key linkage; today show crop + parts table if co-located. |
| 27 | What does the legend/key in Figure 2-1 define for the dashed vs solid lines? | 🟡 | Legend text, if present, is only in free-text description; line-style semantics (dashed=de-energized) may not be reliably captured and aren't structured. Fix: show crop of legend; add figure_legend field for corpora with heavy legend use. |
| 28 | Which one-line diagram covers the North substation 138kV bus? | 🟡 | diagram_category='line_diagram' + applies_to_voltage (mined) + vector search over description helps, but '138kV' and 'North substation' precision depends on tags being mined correctly from surrounding text, which is inconsistent for figure records. Fix: rely on vector+voltage facet; improve figure-level applies_to tags. |
| 29 | Read the insulation class and temperature rise off the motor nameplate in Figure 14-2. | 🟡 | Nameplate prompt lists Insulation class among structured fields but output lands in free-text description; no discrete fields, no confidence. Fix: structured nameplate_insulation_class etc.; show crop and caveat. |
| 30 | Is the disconnect shown open or closed in Figure 8-3? | 🟡 | State is graphical, not text; VLM may describe 'switch shown open' in description but this is exactly where VLMs are unreliable and the system won't assert it. Fix: show crop and let operator confirm; do not assert open/closed. |
| 31 | Which figure has callout number 7? | 🟡 | figure_callouts is NOT searchable/filterable, so you can't query by a callout token directly; diagram_ocr_text IS searchable so a full-text search for '7' works but is hopelessly noisy (matches any '7'). Fix: make figure_callouts (or a numeric callout list) filterable so 'callout eq 7' scoped to a figure works. |
| 32 | Show the wiring diagram and highlight where the neutral bonds to ground. | 🟡 | figure_bbox highlights the WHOLE figure only — line_bboxes/chunk_bboxes for diagrams are just the full figure region (diagram.py:461-478), so the UI can show the figure but cannot highlight the neutral-ground bond specifically. Fix: capture per-callout/per-token bboxes to enable sub-figure highlighting. |
| 33 | What are the dimensions of the trench cross-section in Figure 3-5? | 🟡 | Dimensions appear in diagram_ocr_text if legible; retrievable + shown, but not asserted and no confidence. Fix: show crop; quote dimensions as unverified; add OCR confidence. |
| 34 | The manual says 'see Figure A-1' — pull it up. | ✅ |  |
| 35 | What model of relay is shown in the protection schematic on page 61? | 🟡 | Model string may be in diagram_ocr_text/description (searchable), retrievable via page filter + search, but not asserted and not structured. Fix: show crop; quote model with caveat. |
| 36 | Which figure shows the correct PPE / cover-up configuration for live-line work? | 🟡 | diagram_description embedding + applies_to_domain/hazard_class tags (mined from figure text) can surface it, but safety figures are often photos with little OCR text, so retrieval leans entirely on VLM description quality. Fix: ensure equipment_photo category isn't dropped; boost via safety scoring; show crop. |
| 37 | Read the DIP switch settings shown in Figure 15-7. | 🟡 | DIP positions are graphical; VLM read is unreliable and not asserted. diagram_ocr_text may capture printed labels but not switch positions. Fix: show crop and let operator read; do not assert. |
| 38 | I only remember it's Figure 18-something about a cutout — which figure is it? | 🟡 | Can vector-search 'cutout' over descriptions and facet figure_number, but partial-number matching ('18-*') needs a prefix filter on figure_number/figures_referenced_normalized; normalized form '18xxx' supports startswith. Workable but relies on figure_ref being populated. Fix: expose prefix search on normalized figure number. |
| 39 | What is written in the title block of drawing D-4471? | 🟡 | Title-block text lands undifferentiated in diagram_ocr_text; there's no structured title-block parse (drawing_number/scale/rev), and document_number is doc-level not per-drawing. Fix: add title-block field extraction for engineering drawings; today surface OCR text and show crop. |
| 40 | Show me all nameplate figures in this manual. | ✅ |  |
| 41 | In Figure 10-2 there's a table drawn as an image — what's in row 2? | 🟡 | diagram_category='table_image' is recognized but table-as-image content only lands in free-text diagram_ocr_text with no row/column structure (it never went through shape-table). Fix: route table_image figures back through table extraction; today show crop + raw OCR text. |
| 42 | This nameplate figure looks identical to one in another manual — are the values actually from THIS transformer? | 🟡 | Cross-PDF phash dedup (diagram.py:797-823, opt-in) can copy description/ocr from a DIFFERENT manual's visually-identical figure, so the transcribed serial/rating may be another asset's. No flag marks a borrowed description. Fix: stamp a 'description_source_parent_id'/borrowed flag and never reuse OCR of unique fields (serial) across parents. |
| 43 | What flow direction do the arrows show in the gas schematic Figure 5-9? | 🟡 | Arrow direction is graphical; block_flow prompt asks VLM to describe connections/arrows into description, but direction is unreliable to assert. Fix: show crop; quote described flow as unverified. |
| 44 | Give me the exact text of every warning label visible inside Figure 20-1. | 🟡 | In-figure warning text lands in diagram_ocr_text verbatim (searchable) and figure_callouts, and safety_callout/callouts are mined from it — good for retrieval — but there's no OCR confidence to flag a partially-read WARNING and values aren't asserted. Fix: show crop of the label region; add diagram OCR confidence; treat safety label transcription as show-and-verify. |

## text-facts

| # | Question | | Gap & fix (if not yes) |
|---:|---|:--:|---|
| 1 | What is the Minimum Approach Distance (MAD)? Define it for me. | 🟡 | No defined_term/definition_text field; content_class enum has no 'definition' value (0 hits in skillset). Vector/BM25 may return a chunk that USES 'MAD' rather than the one that DEFINES it. Fix: add a glossary-extraction skill emitting defined_term + definition_text + citation, and a content_class='definition' tag boosted for 'what is/define' queries. |
| 2 | How does the manual define a 'qualified employee'? | 🟡 | Same missing glossary structure; the phrase 'qualified employee' recurs throughout prose so retrieval often surfaces a requirement that mentions it, not the definition. Fix: glossary skill + prefer record_subtype/content_class='definition' when the query is a define/what-is pattern. |
| 3 | What does EPZ (equipotential zone) stand for and where is it defined? | ❌ | No acronym/abbreviation field; 'definition' appears 0 times in skillset. If the doc only expands EPZ once in a heading, ada-002 semantic match is weak on bare acronyms. Fix: add acronym_expansions extraction (acronym -> full form + first-defined citation). |
| 4 | Quote, word for word, the WARNING that appears before energizing a circuit. | 🟡 | callouts/safety_callout/highlight_text capture the callout, but verbatim fidelity is threatened by OCR noise (low_confidence_ocr) and the LLM may paraphrase. Fix: store raw verbatim callout text separately from normalized callouts, surface low_confidence_ocr in the answer, and gate verbatim mode on ocr_min_confidence. |
| 5 | Give me the exact wording of the rule prohibiting working alone on energized lines. | 🟡 | is_prohibition/prohibitions/hazard_class capture prohibitions, but prohibitions is a normalized/paraphrased collection, not guaranteed verbatim, and it is not searchable in a way that preserves the source sentence. Fix: add prohibition_verbatim_text tied to highlight_text + printed_page_label so the exact sentence can be quoted and cited. |
| 6 | Define 'de-energized' and how it differs from 'grounded'. | ❌ | No glossary field to retrieve either definition cleanly, and no mechanism to co-retrieve two related definitions. Both terms saturate the corpus as ordinary words. Fix: glossary skill + retrieve-by-term-key so both defined_term records are fetched together for contrast. |
| 7 | What does the manual say is the required clearance from a 500 kV energized line? | 🟡 | The value often lives in a MAD table (record_type=table_row) while the query is phrased as prose; no reconciliation between prose statement and table value, and no normative-requirement flag to rank the governing sentence. Fix: cross-link prose requirement to governing table via topic_id and add requirement_class tag. |
| 8 | Quote exactly the NOTE about inspecting rubber gloves before each use. | 🟡 | 'note'/'notice' are not typed callouts (0 hits); only footnotes are captured. An inline 'NOTE:' is buried inside chunk prose, not a discrete retrievable/citable unit, so retrieval may miss it. Fix: extend callout typing to NOTE/NOTICE with hazard_class='note' and verbatim text. |
| 9 | What is the exact definition of 'live-line work'? | 🟡 | No glossary structure; 'live-line work' appears in many procedural chunks, so the defining sentence is not preferentially ranked. Fix: glossary extraction with content_class='definition' boost for define/what-is intents. |
| 10 | Give me the current approach-distance value only (ignore the old manual). | 🟡 | is_current_revision/effective_date/supersedes_revision support filtering to the current doc (strong), but the value itself is usually a table cell, so text-facts prose retrieval is incomplete without table linkage. Fix: filter is_current_revision=true AND cross-link the prose statement to the governing table_row. |
| 11 | Quote the exact sentence that says who may authorize a clearance. | 🟡 | No normative-modal flag ('shall'/'may'/'required') except prohibitions; the authorizing sentence is retrieved only by semantics and may be out-ranked by nearby procedural text. Fix: add requirement_class/normative_modal flag and verbatim sentence storage. |
| 12 | What does XLPE stand for? | ❌ | No acronym field; if XLPE is expanded once in prose, ada-002 embedding of the 4-letter token retrieves poorly. Fix: acronym_expansions field populated from parenthetical-expansion detection. |
| 13 | What is MAOP (maximum allowable operating pressure) for a gas main? | 🟡 | Acronym MAOP not indexed as an expansion; the definition vs the specific pressure value are different content that isn't co-retrieved. Fix: acronym field + glossary link + table cross-reference for the value. |
| 14 | What is the odorant requirement for natural gas distribution? Cite the manual. | 🟡 | Citation metadata is strong (document_number, printed_page_label, section_path), but the requirement sentence has no normative flag so it competes with general prose; retrieval may return context, not the rule. Fix: requirement_class tag to boost the governing sentence. |
| 15 | Reproduce word-for-word the DANGER statement on the manual's cover/first page. | 🟡 | Cover/front-matter may be classified as low-value and dropped by retrieval_eligible, and OCR of a stylized cover risks low_confidence_ocr. Fix: ensure front-matter DANGER blocks stay retrieval_eligible and store verbatim text with confidence surfaced. |
| 16 | What is the exact definition of 'restricted approach boundary' and what page is it on? | 🟡 | Page citation is well supported (printed_page_label/physical_pdf_page), but locating the definitional sentence (vs uses) has no glossary support. Fix: glossary skill; page citation already covered. |
| 17 | Find the sentence that uses 'shall' about grounding before starting work, and quote it. | 🟡 | No normative-statement flag; BM25 can keyword-match 'shall'+'ground' but 'shall' is extremely frequent, so precision is poor and the exact governing sentence isn't isolatable as a citable unit. Fix: add requirement_class + sentence-level normative extraction. |
| 18 | What does the footnote under Table 3 say about ambient temperature correction? | 🟡 | footnotes field exists (captured), but there is no explicit link from a footnote to the specific table_number it annotates, so co-retrieving 'Table 3' + its footnote is unreliable. Fix: attach footnotes to the table record via tbl_parent_id/table_number linkage. |
| 19 | Quote the exact CAUTION about working in wet weather. | 🟡 | callouts capture the CAUTION but as normalized text; verbatim fidelity and OCR confidence are not guaranteed, and there is no callout_type filter to isolate CAUTION vs WARNING vs NOTE. Fix: store callout_type + verbatim_text and expose ocr confidence. |
| 20 | What is a 'tailgate meeting' as defined in the manual? | 🟡 | No glossary; the term appears mostly in procedural context, so the defining sentence is not preferentially ranked. Fix: glossary extraction skill. |
| 21 | Verbatim: give me the full definition of 'energized' with exact wording and citation. | 🟡 | highlight_text/text_bbox/line_bboxes support highlighting a span, but the definition may span a chunk boundary and chunk_prev_id/chunk_next_id are empty stubs, so a boundary-split definition cannot be stitched into a complete verbatim quote. Fix: populate neighbor IDs during chunking + glossary capture. |
| 22 | The definition I need runs past the end of one chunk — give me the whole thing. | ❌ | chunk_prev_id/chunk_next_id are empty stubs, so the retriever cannot pull the continuation chunk; the answer is truncated and any 'verbatim' quote is incomplete. Fix: populate chunk_prev_id/chunk_next_id (and/or use layout_ordinal within topic_id) to fetch neighbors. |
| 23 | How often must hot sticks (live-line tools) be tested? One-line answer with citation. | 🟡 | The frequency may be stated in prose or a maintenance table; no normative flag and possible prose/table split mean retrieval may miss the governing statement. Fix: requirement_class tag + topic_id cross-type grouping so prose and table co-retrieve. |
| 24 | What is the minimum number of workers required for a switching operation? | 🟡 | No normative-statement flag; the number is embedded in prose and competes with surrounding procedure text. Citation is fine once found. Fix: requirement_class tagging. |
| 25 | Does the electric manual and the gas manual define 'qualified' the same way? | ❌ | No glossary field and no cross-document conflict surfacing; the two definitions live in separate docs (document_family_id differs) with no linkage. Fix: glossary skill keyed by term + a cross-doc consistency check on defined_term. |
| 26 | Old revision said 10 ft, the new one says 8 ft — quote both and tell me which is current. | 🟡 | is_current_revision/supersedes_revision identify the current one, but superseded revisions are typically not retrieval_eligible/indexed, so the old value cannot be quoted. Fix: index superseded revisions with is_current_revision=false and keep them retrievable for comparison. |
| 27 | What does the manual say about wearing a metal wristwatch or ring near energized equipment? Quote it. | 🟡 | Captured as is_prohibition/prohibitions but normalized, not guaranteed verbatim, and the specific object (wristwatch/ring) may not be in the normalized prohibition text. Fix: prohibition_verbatim_text + object extraction. |
| 28 | Is a clearance the same as a 'release'? Give the definitions. | ❌ | No glossary; both are common words in the corpus, so definitional sentences are not isolable, and there is no co-retrieval of related terms. Fix: glossary skill with related-term grouping. |
| 29 | What's the exact phrase the manual uses for emergency shutdown of gas flow? | 🟡 | No terminology/defined-term index; ada-002 paraphrase-matching may return a description rather than the exact official phrase. Fix: glossary/term extraction to capture canonical phrasing verbatim. |
| 30 | Quote the requirement that two people must independently verify a de-energized state. | 🟡 | No normative flag; retrieval is semantic-only and the exact sentence isn't a citable unit distinct from surrounding procedure. Fix: requirement_class + verbatim sentence storage. |
| 31 | Is the required clearance 3 feet or 36 inches? They seem inconsistent. | ❌ | No unit normalization field; the index stores raw text so ft vs in equivalence is left to the LLM with no cross-statement reconciliation. Fix: extract normalized numeric value + unit for measured requirements. |
| 32 | Per Section 4, what is the definition of 'grounding'? | 🟡 | section_path/chapter_label/header_* allow scoping to Section 4 (strong), but locating the definition within it has no glossary support. Fix: glossary skill; section scoping already covered. |
| 33 | Quote the note about which arc-flash PPE category applies. | 🟡 | Inline NOTE not typed as a callout (0 'note' hits); the content is inside chunk prose only. hazard_class may tag arc-flash but not isolate the note. Fix: NOTE/NOTICE callout typing with verbatim text. |
| 34 | What does 'OQ' (Operator Qualification) mean? | ❌ | No acronym field; header_* are searchable but a bare 'OQ' query embeds poorly and may not match the heading. Fix: acronym_expansions extraction including heading-defined acronyms. |
| 35 | What is the required color code for grounding cables/clamps? One line, cite it. | 🟡 | Likely a table/spec value; prose text-facts retrieval may miss it and there is no prose-to-table linkage for the citation. Fix: topic_id cross-type grouping so the spec value + governing prose co-retrieve. |
| 36 | Quote the label text on the warning sign shown in Figure 5. | 🟡 | diagram_ocr_text + figure_number capture in-figure text (good coverage), but OCR fidelity is uncertain (ocr_min_confidence/low_confidence_ocr) so a claimed 'verbatim' quote may contain OCR errors. Fix: surface OCR confidence and flag low-confidence verbatim as approximate. |
| 37 | Define Class 00 rubber gloves and give their maximum use voltage. | 🟡 | No glossary for the class definition; the max-use voltage is a glove-class table value not linked to the definitional prose. Fix: glossary skill + link class term to its spec table_row via table_row_semantic_key. |
| 38 | What page defines 'affected employee' — just give me the citation. | 🟡 | Page/section citation is well supported, but identifying the DEFINING occurrence (vs the many using occurrences) requires glossary tagging that doesn't exist. Fix: glossary skill records the defining location precisely. |
| 39 | Quote the exact prohibition on using metal ladders near overhead lines. | 🟡 | is_prohibition/prohibitions capture it but normalized; the specific object (metal ladder) and exact wording aren't guaranteed preserved for a verbatim quote. Fix: prohibition_verbatim_text tied to highlight_text + page citation. |
| 40 | Give the one-line rule for the maximum wind speed at which crane work near lines must stop. | 🟡 | No normative flag and the threshold may sit in a table; retrieval may return general crane guidance instead of the stop-work threshold. Fix: requirement_class + normalized numeric threshold extraction. |
| 41 | Answer in one sentence: who is responsible for issuing the switching order? Cite doc number and page. | 🟡 | Citation fields (document_number, printed_page_label) fully support the cite; isolating the single responsibility sentence lacks a normative/role flag so precision suffers. Fix: requirement_class + role attribution tagging. |
| 42 | What is the maximum pressure allowed for a gas service line? Cite the exact page label. | 🟡 | printed_page_label and printed_page_label_is_synthetic give proper human-page citation (strong), but the pressure value is a table cell so prose text-facts retrieval alone may miss it. Fix: cross-link prose to governing table via topic_id. |
| 43 | Just confirm yes/no with a citation: does the manual require a second ground on the source side? | 🟡 | No normative flag to reliably surface the governing sentence; a yes/no without the exact supporting sentence risks an uncited/hallucinated affirmation. Fix: requirement_class tagging so the confirming sentence is retrievable and citable. |
| 44 | Give me the manual's exact wording for what counts as an 'incident' that must be reported. | 🟡 | No glossary/definition tagging; the scoping sentence competes with reporting-procedure prose and verbatim fidelity depends on OCR. Fix: glossary/definition extraction + verbatim text with OCR confidence surfaced. |
| 45 | I asked for a verbatim quote but the answer looks paraphrased — can the system guarantee exact text? | 🟡 | highlight_text provides a highlightable span but is chunk-level and may be normalized; there is no explicit verbatim-vs-normalized distinction and low_confidence_ocr isn't surfaced to the user. Fix: store a raw verbatim span separate from normalized text and gate/annotate verbatim answers by ocr_min_confidence. |

## summaries

| # | Question | | Gap & fix (if not yes) |
|---:|---|:--:|---|
| 1 | What is this manual about? | ✅ |  |
| 2 | Give me a one-paragraph overview of the underground residential distribution manual. | ✅ |  |
| 3 | Summarize the grounding and bonding chapter. | ❌ | Only a whole-doc summary exists; there is no chapter-scoped summary record. Bot must retrieve all chapter chunks (header/chapter_label filter) and summarize live, risking truncation and missed safety notes. Fix: emit record_type=summary at chapter granularity keyed by topic_id/chapter_number, each with a physical_pdf_page range for citation. |
| 4 | What topics are covered in this manual? | 🟡 | Section titles are fed into the summarizer (capped at 40) but NOT stored as a retrievable field; only header_1/2/3 and chapter_label facets exist, which are per-chunk and unordered. Answer is a lossy paraphrase from prose. Fix: persist the ordered section_titles/TOC list as a doc-level field. |
| 5 | Show me the table of contents. | ❌ | No TOC record exists. chapter_label/section_path/header facets can be scraped but ordering across chunks is not guaranteed and appendices/sub-sections are missed. Fix: build a doc-level TOC field (ordered chapter_number + title + start page) during indexing. |
| 6 | What's the scope of this document? | 🟡 | The scope statement (usually §1) is only reflected as paraphrase inside the generated summary and is not stored verbatim; citing 'the manual states its scope as…' risks paraphrase drift. Fix: extract and store scope_statement verbatim with its page from chapter 1. |
| 7 | What equipment does this manual apply to? | ✅ |  |
| 8 | What voltage classes does this manual cover? | 🟡 | applies_to_voltage is stamped on the summary but derived from the ~20k sampled text, so a voltage class only referenced in an un-sampled middle chapter can be omitted. Fix: aggregate applies_to_voltage across ALL child chunks of the parent, not just the summary text. |
| 9 | Is this manual for gas or electric work? | ✅ |  |
| 10 | Summarize chapter 4. | 🟡 | chapter_number is filterable so the bot can pull chapter 4's chunks and summarize on the fly, but there is no pre-built chapter summary; long chapters exceed context and the answer is uncited to a page range. Fix: chapter-granularity summary records with page spans. |
| 11 | Give me a detailed section-by-section summary of the whole manual. | ❌ | Only a single 300-500 word whole-doc summary exists, built from a sampled slice. Section-by-section coverage is impossible today. Fix: per-section summary records ordered by layout_ordinal. |
| 12 | What are the main safety hazards covered in this manual? | 🟡 | hazard_class/criticality/governing_callouts are stamped on the summary but mined only from the sampled summary text, so a hazard confined to an un-sampled chapter is dropped — a safety-completeness risk. Fix: roll up hazard_class/governing_callouts from all child records of the parent onto the summary. |
| 13 | Which procedures are in this manual? | 🟡 | procedure_title/procedure_id exist per procedure record and are facetable, so a list can be assembled by filtering parent_id, but the summary itself only names 'main procedures' in prose and there is no stored count/list on the summary. Fix: store procedure_titles[] and procedure_count on the summary record. |
| 14 | List all the figures and diagrams in this manual. | 🟡 | figure_number/figure_title records are filterable by parent, so an enumeration is possible, but that is a listing operation, not a summary field; the summary only names 'notable diagrams.' Fix: store figure_count and figure list on the summary for one-shot answers. |
| 15 | What changed in this revision compared to the previous one? | ❌ | supersedes_revision and is_current_revision exist as pointers but there is no changelog text or per-revision diff; the summary does not compare versions. Fix: capture and index the manual's revision-history/change-summary block as a dedicated field. |
| 16 | Give me an executive summary of this manual for a management briefing. | ✅ |  |
| 17 | Summarize this manual at a high level for a new apprentice. | ✅ |  |
| 18 | What codes and standards does this manual reference? | ❌ | No field captures cited standards (NESC, OSHA, ANSI, etc.); they may sit in footnotes/body text but are not extracted or summarized. Fix: extract standard citations into a referenced_standards collection at doc and section level. |
| 19 | Does this manual cover energized/live-line work? | 🟡 | Answerable only if 'live-line' surfaces in the sampled summary text or a searchable chunk; there is no explicit coverage flag, so a negative answer can't be trusted (absence in the sample ≠ absence in the manual). Fix: aggregate applies_to_* and a topics-covered list from all child chunks. |
| 20 | Summarize everything we have across our electric distribution manuals. | 🟡 | Filtering summary records by applies_to_domain=electric returns each doc's summary, but the bot must aggregate them and recall is bounded by top-k; there's no corpus-level summary. Fix: support faceted enumeration of all summary records and stitch, or precompute a corpus digest. |
| 21 | How long is this manual / how many pages? | ✅ |  |
| 22 | When was this manual last revised and what's its effective date? | ✅ |  |
| 23 | Is this the current version of the manual? | ✅ |  |
| 24 | Summarize section 3.2. | 🟡 | section_path is filterable so 3.2's chunks can be pulled and summarized live, but there is no pre-built sub-section summary and no page-span citation on the result. Fix: optional section-level summary records keyed by section_path with page ranges. |
| 25 | What PPE is required across this manual? | 🟡 | PPE mentions can be searched in callouts/chunks but there is no curated doc-level PPE overview; completeness depends on retrieval recall, unsafe for a definitive list. Fix: a topic-rollup summary per hazard/PPE theme keyed by topic_id. |
| 26 | Give me the key takeaways from this manual. | 🟡 | The summary provides takeaways but is built from a sampled slice, so critical mid-document safety notes can be absent from the 'key points' — a completeness risk for safety. Fix: bias the summarizer input toward high-criticality/safety_callout child chunks rather than a positional sample. |
| 27 | Which chapters deal with grounding? | 🟡 | header/chapter_label search can surface matching chapters, but there is no stored topic→chapter index and no chapter summary to confirm relevance; ranking is text-match, not curated. Fix: topic_id grouping surfaced as a doc-level topic map with chapter labels. |
| 28 | What's covered in the appendices? | ❌ | Appendices land in the tail 6k sample so they are only thinly represented in the whole-doc summary, and there's no appendix-scoped summary record. Fix: treat appendices as sections with their own summary records. |
| 29 | Summarize only the gas portions of this combined gas/electric manual. | ❌ | The single summary is whole-doc; it cannot be sliced to only applies_to_domain=gas sections. Fix: per-section summaries carrying applies_to_domain, so they can be filtered and re-summarized by domain. |
| 30 | Which manual should I use for underground residential distribution? | 🟡 | document_title + the summary act as a routing signal, but there is no explicit routing/keywords field, so selection relies on semantic match to the summary. Fix: add a curated document_scope/keywords field to make routing deterministic. |
| 31 | Who does this manual apply to — what crews or roles? | 🟡 | Role/audience applicability isn't a stored field; only reflected as paraphrase in the summary if it appeared in the sample. Fix: extract the applicability/'who this applies to' statement verbatim into a field. |
| 32 | Summarize the introduction/preface of the manual. | 🟡 | The head-8k sample covers the intro so first chunks are retrievable and summarizable, but there's no discrete intro summary record and no page-span citation. Fix: section summary for front matter. |
| 33 | Give me a bullet list of every safety warning in the manual. | 🟡 | callouts/governing_callouts/prohibitions exist per record and are filterable by parent, so a list can be built, but 'every' requires exhaustive retrieval beyond top-k and the summary itself doesn't hold the full set. Fix: a doc-level safety_callouts rollup field aggregated from all children. |
| 34 | What's the purpose of this document? | 🟡 | Purpose is only paraphrased in the summary and not stored verbatim; safe citation isn't possible. Fix: extract purpose_statement verbatim with page. |
| 35 | What's the difference between the two grounding manuals we have? | ❌ | Both doc summaries are retrievable but there is no comparison capability; any 'difference' is LLM-inferred from two paraphrased summaries — unreliable for safety. Fix: structured scope/topic fields per doc to enable deterministic comparison. |
| 36 | Summarize everything about transformers in this manual. | 🟡 | applies_to_equipment=transformer + topic_id can gather relevant chunks to summarize live, but recall/completeness is not guaranteed and the result isn't a stored, citable summary. Fix: topic_id-scoped summary records. |
| 37 | Does this manual include emergency or first-aid procedures? | 🟡 | Only answerable if the topic appears in the sampled summary or a retrieved chunk; a 'no' is not trustworthy. Fix: doc-level topics-covered list built from all child sections. |
| 38 | What are the document number and title of this manual? | ✅ |  |
| 39 | Read me the manual summary and cite the page it's on. | 🟡 | The summary has page_resolution_method='document_summary' and NO physical_pdf_page, so it can only be cited to the whole document, not a page. The generated summary is also paraphrase, so page-level citation would be misleading anyway. Fix: cite underlying chunks; optionally stamp a start-page range on the summary. |
| 40 | Word-for-word, what does the manual's scope statement say? | ❌ | The summary record is LLM paraphrase (non-verbatim). Serving it verbatim would misquote the manual. The actual scope chunk exists as a text record but isn't tagged as 'scope', so it isn't reliably retrievable for a verbatim answer. Fix: tag/extract the scope statement verbatim and route verbatim requests to text records, never the summary. |
| 41 | Summarize the manual that our indexer couldn't fully process. | ❌ | When summary generation fails or the PDF has no extractable text, the summary record has empty chunk, retrieval_eligible=false and processing_status='no_content'/'summary_error', so no overview can be returned and the bot may silently have nothing. Fix: surface the failure to the user and add an OCR/heal fallback for these docs. |
| 42 | Give an overview of the switching chapter including its figures and tables. | 🟡 | topic_id groups text/diagram/table records of a section, enabling assembly, but there is no pre-built cross-type summary and no single citable record; the bot must gather and synthesize live. Fix: topic_id-level summary records that reference their figures/tables. |
| 43 | What qualifications or prerequisites does this manual require? | 🟡 | Prerequisites/qualifications aren't a stored field; only found if they surfaced in the sampled summary or a searched chunk. Fix: extract prerequisites into a field or a topics-covered list. |
| 44 | Summarize this manual in Spanish. | 🟡 | language field exists but stored content is English; the doc summary can be translated LLM-side at answer time, so this works for the overview but not for citing verbatim non-English text. Acceptable for summaries; flag translation as non-authoritative. |
| 45 | How many procedures, tables, and figures are in this manual? | 🟡 | Counts can be derived by faceting record_type/procedure_id/table_number/figure_number under a parent, but there are no stored aggregate counts on the summary, so it requires multiple facet queries. Fix: stamp procedure_count/table_count/figure_count on the summary record. |
| 46 | Give me a TL;DR of the arc-flash section. | 🟡 | Retrievable via header/callout search then live summarization, but no pre-built arc-flash section summary and no page-span citation. Fix: section/topic summary records with page ranges. |
| 47 | Which parts of this manual are the most safety-critical? | 🟡 | criticality/hazard_class are per-record facets, so high-criticality sections can be enumerated, but the summary doesn't rank sections and there's no stored criticality map. Fix: roll up a criticality-ranked section list onto the summary. |
| 48 | Summarize this manual as of its 2019 revision. | 🟡 | If the 2019 revision was indexed it has its own summary reachable via is_current_revision=false + document_revision/effective_date filters, but superseded docs may not be retained and default retrieval favors current. Fix: retain superseded summaries and support revision-scoped filtering explicitly. |
| 49 | Is there anything in this manual about confined-space entry? | 🟡 | A hit in a searchable chunk confirms presence, but absence in top-k or in the sampled summary can't confirm the topic is genuinely absent. Fix: a complete topics-covered list per document so 'not covered' is authoritative. |
| 50 | What's the overall structure and organization of this manual? | 🟡 | header_1/2/3, section_path, chapter_label and layout_ordinal exist per chunk, so an outline can be reconstructed, but it isn't stored as an ordered doc-level structure and reconstruction is error-prone across many chunks. Fix: persist an ordered outline/TOC field at index time. |

## highlighting-citation

| # | Question | | Gap & fix (if not yes) |
|---:|---|:--:|---|
| 1 | Show me on the page where it says to ground both sides of the cut before working. | ✅ |  |
| 2 | Highlight this whole paragraph, not just one line. | ✅ |  |
| 3 | This answer runs across the bottom of page 12 and top of page 13 — highlight both parts. | ✅ |  |
| 4 | The rule continues past where you highlighted — show me the rest of it on the next page. | ❌ | chunk_next_id/chunk_prev_id are empty-string stubs (page_label.py:2441-2442), so there is no forward link to the continuation chunk. Fix: populate chunk_prev_id/chunk_next_id in reading order per topic_id/layout_ordinal so the frontend can pull and highlight the neighbor. |
| 5 | Take me to that page in the manual. | 🟡 | source_url exists but isn't populated with a page fragment; deep-link must be assembled frontend-side as source_path + '#page=' + physical_pdf_page. Fix: either populate source_url with the page-anchored URL at index time, or document the assembly contract so every client builds it consistently. |
| 6 | What page is this on? | 🟡 | Both a physical PDF page and a printed label exist, but they differ (front-matter offset) and the label may be synthetic. Fix: the answer layer must choose printed_page_label for human citation and physical_pdf_page for the viewer jump, and suppress/caveat when printed_page_label_is_synthetic is true. |
| 7 | Cite this with the chapter and page so I can log it. | ✅ |  |
| 8 | Which revision of the manual is this from, and is it the current one? | ✅ |  |
| 9 | Crop out Figure 4-2 and show me just the diagram. | ✅ |  |
| 10 | In that diagram, circle the callout that points to the bushing. | ❌ | figure_callouts is a text collection with no geometry; only the whole figure_bbox exists. Fix: capture per-callout bounding boxes (from OCR/layout) as a parallel collection so a single callout can be accented within the figure crop. |
| 11 | Highlight the row in Table 3 for #2 AWG copper. | 🟡 | table_row records inherit the parent table_bbox and explicitly do not compute per-row boxes (process_table.py:280-283), so only the whole table can be outlined. Fix: compute per-row bboxes during table extraction (or store row y-bands) so a single row can be tightly highlighted; interim UX is dim-table + note the row key. |
| 12 | Show me exactly which cell has the 15-inch clearance value. | ❌ | No cell-level geometry exists; table_row_cells is text only and rows have no bbox. Fix: persist cell bounding boxes from the table layout so an individual cell can be highlighted; otherwise fall back to citing table_number + row semantic key. |
| 13 | Jump to the page and highlight the WARNING box about arc flash. | 🟡 | safety_callout/callouts flag the chunk and highlight_text can match the warning text, so a text-line highlight is possible, but there is no dedicated callout-box bbox. Fix: if the WARNING is its own chunk its chunk_bboxes suffice; where callouts are inline, add a callout-region bbox so the boxed hazard can be outlined precisely. |
| 14 | Which section and heading does this come from? | ✅ |  |
| 15 | Give me a citation I can paste: document number, revision, page, and figure. | ✅ |  |
| 16 | This looks like an old version — take me to the same rule in the current revision and highlight it. | ❌ | document_family_id + supersedes_revision identify the lineage, but there is no guaranteed stable content anchor mapping the same rule across revisions, and topic_id is per-index-run. Fix: mint a revision-stable clause/topic key so the same provision can be located and highlighted in whichever revision is current. |
| 17 | Show me the printed page number, not the PDF page. | ✅ |  |
| 18 | The page number you cited doesn't match the footer on the page. | 🟡 | Both values exist plus page_resolution_method, but the answer layer must present them correctly (printed for the reader, physical for the viewer jump) and explain synthetic labels. Fix: standardize citation format to always show the printed label and reserve physical page for the deep link. |
| 19 | Highlight the same clause on all three pages where this table repeats. | 🟡 | table_split_index/count and multi-page table_bbox let the frontend outline the table on each page, but without per-row bboxes it cannot pinpoint the repeated clause row. Fix: add per-row geometry (see row-highlight fix) so the specific row is accented on each split page. |
| 20 | Open the figure that spans two pages and highlight both halves. | ✅ |  |
| 21 | Draw the box a little looser — it's cutting off the last word. | ✅ |  |
| 22 | The highlight is in the wrong spot on the page. | 🟡 | bbox_version (2.0.0) and page_width_in/height_in exist, but the origin convention (top-left vs bottom-left, inches) is not carried as data. Fix: document/version the coordinate contract explicitly (origin, units, DPI-independence) so every client renders bboxes identically; diagnose_highlight.py should assert it. |
| 23 | This page is rotated (landscape scan) — the highlight is sideways. | ❌ | No page-rotation field is stored, so the frontend cannot know to transform bbox coordinates for rotated pages. Fix: persist page rotation (0/90/180/270) per page so overlays and crops can be rotated to match the rendered view. |
| 24 | That figure was OCR'd with low confidence — is the highlight reliable? | ✅ |  |
| 25 | Show me where in the diagram it reads '600 A max'. | ❌ | diagram_ocr_text is a flat string with no per-span geometry, so a phrase inside the image cannot be located. Fix: store word/line bboxes for OCR'd figure text so specific in-image strings can be highlighted. |
| 26 | Give me a link that opens the PDF straight to this figure. | 🟡 | Constructible from source_path + physical_pdf_page, but there is no named-destination anchor to land on the figure itself (only the page). Fix: populate source_url with the page fragment; optionally add PDF named destinations for figures/tables for exact landing. |
| 27 | Which document is this from — full title, not just the file name? | ✅ |  |
| 28 | Cite the effective date of this procedure. | ✅ |  |
| 29 | Highlight step 4 of the switching procedure on the page. | 🟡 | If the step is its own chunk, highlight_text + line_bboxes cover it; but steps grouped into one chunk share one bbox and can't be isolated by procedure_step_order. Fix: when steps are co-located in a chunk, retain per-step line offsets/bboxes so an individual step can be highlighted. |
| 30 | Show me the exact words to read aloud in the tailboard, and where they are on the page. | ✅ |  |
| 31 | Point to the note that this clearance does NOT apply to gas mains. | 🟡 | is_prohibition and prohibitions flag the exception and highlight_text can match it if the prohibition text equals the highlight string, but prohibitions is a derived list that may not be verbatim-locatable. Fix: ensure each prohibition retains a verbatim span + bbox so the exact exception sentence can be highlighted. |
| 32 | Which page and section is Figure 4-2 referenced from in the text? | ✅ |  |
| 33 | Take me to Table 5-1 by its number and highlight the caption. | ✅ |  |
| 34 | Highlight everything on this page that's a safety warning. | 🟡 | Chunks can be filtered by page and safety_callout, and chunk_bboxes locate each, but inline warnings that aren't standalone chunks lack a callout-region bbox. Fix: add callout-region bboxes so every WARNING/CAUTION/DANGER block on a page can be outlined. |
| 35 | Show the surrounding text around the highlight for context. | 🟡 | surrounding_context (not retrievable) and chunk text give in-chunk context, but true previous/next chunk context is unavailable because chunk_prev_id/next_id are empty. Fix: make surrounding_context retrievable and/or populate the neighbor links. |
| 36 | Which chapter number is this in? | ✅ |  |
| 37 | Export this page region as an image for my JSA packet. | 🟡 | The geometry to crop exists (chunk_bboxes/figure_bbox with page dimensions), but the index only supplies coordinates — the actual PDF-to-image render must exist client/server-side. Fix: confirm a render service consumes these bboxes; index side is sufficient. |
| 38 | Highlight the same requirement in both the electric and gas manuals side by side. | ❌ | topic_id groups within one document/run only; there is no cross-document concept linking equivalent clauses, and per-doc geometry is fine. Fix: add a shared clause/topic taxonomy across documents to co-locate and dual-highlight equivalent requirements. |
| 39 | This citation shows page 7 but the manual has no page 7 printed — explain. | ✅ |  |
| 40 | Highlight the exact sentence, but the retrieved chunk is a big merged block — I only want the one line. | ✅ |  |
| 41 | Jump me to the last page of this multi-page procedure and highlight the sign-off line. | 🟡 | physical_pdf_page_end gives the last page and line_bboxes on that page work if the sign-off is in the same chunk, but if it's a separate trailing chunk the missing chunk_next_id prevents reaching it. Fix: populate neighbor links to traverse to the final chunk. |
| 42 | Show me the page thumbnail with the answer region marked. | 🟡 | Normalized bboxes support scaling to a thumbnail, but no thumbnail asset is produced by the index. Fix: this is a render-layer responsibility; index geometry is sufficient — confirm the frontend scales bbox by thumbnail/page-dimension ratio. |
| 43 | Is what you highlighted actually on the page you cited, or did the chunk get mismapped? | 🟡 | Each bbox carries a page field and diagnose_highlight.py exists, but there is no runtime assertion that bbox pages equal physical_pdf_page(s). Fix: validate at index time that every bbox page is within physical_pdf_pages and set retrieval_eligible_reason/quality flag on mismatch. |
| 44 | Cite this so an auditor can find it without the app — chapter, printed page, revision, and effective date. | ✅ |  |
| 45 | Highlight the OCR text overlay only where confidence is high, and grey out the uncertain parts. | ❌ | Only a chunk-level ocr_min_confidence and low_confidence_ocr flag exist — no per-span confidence or geometry. Fix: store per-line/word OCR confidence alongside per-span bboxes to drive graded highlight styling. |
| 46 | Take me to where this figure is defined, not just where it's mentioned. | ✅ |  |

## safety-hazard-ppe

| # | Question | | Gap & fix (if not yes) |
|---:|---|:--:|---|
| 1 | What PPE do I need to change out a 13.2 kV pole-mount transformer? | 🟡 | No dedicated PPE field. PPE lists live as free text/table rows retrieved only via vector+BM25; there is no `required_ppe` collection to guarantee completeness or filter by task. applies_to_voltage='13.2kV'/'medium_voltage' and applies_to_equipment='transformer' help scope, but a PPE list spread across a bullet list or a table can be partially retrieved. Fix: add a `required_ppe` Collection(Edm.String) extracted from PPE/'personal protective equipment' sections and rubber-goods tables, linked by topic_id, so the answer is assembled from a structured field not a lucky chunk. |
| 2 | Is it allowed to work barehanded on 4kV primary? | 🟡 | is_prohibition/prohibitions capture 'do not / never / not permitted' clauses, but detect_prohibitions truncates at newline/semicolon and 180 chars (`[^\n.;]{0,180}`), so a multi-line boxed prohibition returns only the first line — an incomplete/altered safety rule. Fix: capture the full boxed callout body (join wrapped lines before regex) and store the untruncated clause; add a prohibition-scope tag so 'barehand' maps to the right voltage. |
| 3 | What's the minimum approach distance for 34.5 kV? | 🟡 | MAD values live in table_row records reachable via table_row_semantic_key/value + vector, but there is no normalized numeric approach-distance field keyed by voltage band, so retrieval depends on the row text literally containing '34.5' and the row vector landing. Voltage 34.5 falls between common table breakpoints (15/25/46 kV) — the bot may return the wrong band. Fix: emit a structured `approach_distance` (voltage_class, phase_to_ground, phase_to_phase, unit) parsed from MAD tables, filterable by band. |
| 4 | Show me the DANGER notice that applies to step 6 of the recloser bypass procedure. | 🟡 | governing_callouts is section-scoped and procedure_id/procedure_step_order link steps, so a WARNING/DANGER at the top of the section is inherited by step 6 — good. But the callout body is truncated to the first line by SAFETY_CALLOUT_RE (`[^\n]{1,200}`), so a multi-line DANGER box is returned incomplete. And a DANGER on a different page than the step's section chunk is not inherited (governing scope = same section only). Fix: de-truncate callout extraction and widen governing scope to the whole procedure_id, not just the layout section. |
| 5 | What arc-flash PPE category do I need for a 480V panel? | 🟡 | hazard_class='arc_flash' tags the chunk and the arc-flash/PPE-category table is retrievable as table_row records, but there is no normalized field mapping (equipment class -> incident energy cal/cm2 -> PPE category). Answer relies on the row text matching '480' and the category cell surviving table shaping. table_row emits safety_callout but NOT the `callouts` keyword field, so a callout-filtered retrieval misses these rows. Fix: emit `callouts` on table rows too, and add an arc_flash_ppe_category / incident_energy structured field. |
| 6 | Can I open a load-break elbow on an energized 25kV URD cable? | 🟡 | applies_to_equipment='cable' and applies_to_voltage='25kV'/'medium_voltage' scope it, and is_prohibition surfaces any 'do not' rule, but if the rule is phrased conditionally ('only after de-energizing', 'shall be de-energized before') it is NOT caught by _PROHIBITION_RE (no do-not/never trigger), so the governing restriction may be missed. Fix: extend prohibition detection to conditional/mandatory forms ('shall be ... before', 'only when', 'required to de-energize') and tag them as conditional_prohibition. |
| 7 | Gas reading is 20% LEL in a manhole — what do I do? | 🟡 | hazard_class='gas'/'confined_space' tags the content and the response steps are retrievable as procedure/table_row records, but there is no numeric threshold model (e.g. band 10-20% LEL -> action). Retrieval depends on the row/text containing '20%' or the band. If thresholds are in a table, the correct row must vector-match and survive shaping; and table rows lack the callouts keyword. Fix: parse gas-threshold response tables into keyed rows (min_pct, max_pct, action) and add callouts to rows so DANGER-gated actions are filterable. |
| 8 | What are all the hazards involved in a 4kV manhole cable splice? | 🟡 | hazard_class is a real recall-leaning multi-valued field (live_line, energized, confined_space, gas, chemical for PCB, etc.) and is facetable, so the bot can enumerate hazards present in retrieved chunks. Gap: hazards are per-chunk, and there is no topic-level rollup — hazards named only in a figure OCR or a separate WARNING page may not co-retrieve with the splice text. Fix: aggregate hazard_class up to topic_id so a task-level hazard summary is complete. |
| 9 | Is rubber glove class 2 rated for 17,000 volts? | 🟡 | The ASTM rubber-glove class/voltage table is indexed as table_row records with table_row_semantic_key/value, retrievable by vector+BM25, but there is no structured glove_class->max_use_voltage field, so correctness depends on the exact row being retrieved and cells being intact after shaping. No numeric guard prevents returning the class-2 (17kV) vs class-3 (26.5kV) confusion. Fix: emit a structured protective-equipment ratings table (class, max_use_voltage, proof_test) filterable by class and voltage. |
| 10 | What does the WARNING on page 3-14 say, exactly? | 🟡 | printed_page_label filtering + callouts/governing_callouts retrieve the page and its callouts, but SAFETY_CALLOUT_RE stops at the first newline so a multi-line WARNING box is returned truncated — a verbatim request then returns an altered/partial safety notice. highlight_text/chunk hold the full text but the bot may cite the truncated callout field. Fix: reconstruct boxed multi-line callouts into a single body before extraction; store full verbatim callout text. |
| 11 | Do I need fall protection to work from an aerial bucket at 40 feet? | ✅ |  |
| 12 | Can two workers be in the same trench during shoring installation? | 🟡 | hazard_class='excavation' tags it and is_prohibition catches 'do not / no more than', but many trench rules are stated as affirmative limits ('a competent person shall...', 'only one worker') not prohibition triggers, so is_prohibition misses them. Fix: broaden prohibition/limit detection to affirmative-constraint patterns and expose them via a `restrictions` field distinct from hard prohibitions. |
| 13 | What's the flash protection boundary for this 15kV switchgear? | 🟡 | hazard_class='arc_flash' and applies_to_equipment='switchgear' scope it, but the boundary value is a number in a table/label-study; there is no normalized arc_flash_boundary field, so retrieval depends on the value co-locating with '15kV switchgear' text and surviving table shaping. Fix: extract arc-flash label/study values (boundary_in, incident_energy, ppe_category) into structured fields keyed by equipment class. |
| 14 | Is it OK to use a wooden ladder near energized 12kV conductors? | 🟡 | hazard_class='live_line'/'energized' and is_prohibition can surface a 'do not use metal/conductive ladders' rule, but the question is about WOOD (often the permitted alternative), and the field only stores the prohibition clause, truncated at the first line, so a rule like 'do not use metal ladders; only fiberglass...' loses the permitted-alternative tail. Fix: de-truncate and keep the full clause including the permitted alternative. |
| 15 | What atmospheric tests are required before entering a vault? | 🟡 | hazard_class='confined_space' plus procedure_id/step ordering can return the test sequence (oxygen, LEL, toxic) as ordered steps. Gap: numeric acceptance thresholds (e.g. O2 19.5-23.5%, LEL <10%) are often in an adjacent table whose rows lack the callouts keyword and aren't step-linked, so the thresholds may not co-retrieve with the steps. Fix: link threshold table rows to the entry procedure via topic_id and add numeric threshold fields. |
| 16 | Which steps of the transformer changeout require rubber gloves? | ❌ | There is no per-step PPE linkage. PPE is stated once at the procedure top or in a callout; procedure_step_text holds the step, but nothing maps 'rubber gloves' to step numbers. governing_callouts gives section-level PPE, not per-step. Fix: add a step-level `step_ppe`/`step_hazard` enrichment in procedures.py that propagates the governing PPE/callout to each procedure_step_id. |
| 17 | Am I allowed to re-energize a line after a fuse operates without patrolling it? | 🟡 | is_prohibition may catch 'do not re-energize/reclose until...' but if phrased 'the line shall be patrolled before re-energizing' it is a mandatory-condition, not a do-not trigger, so it is missed. applies_to_domain='electric' helps but there is no reclose-policy tag. Fix: add conditional/mandatory-rule detection and a reclose_policy hazard subtag. |
| 18 | What are the DANGER callouts anywhere in the live-line work chapter? | 🟡 | callouts keyword field + chapter_label filter supports callouts/any(c: c eq 'DANGER') on text/diagram chunks, and safety-boost ranks them. Two gaps: (1) table_row records set safety_callout but never emit the callouts keyword, so DANGER inside a table is invisible to this filter; (2) NOTE/NOTICE are treated as callouts by SAFETY_CALLOUT_RE inflating results. Fix: emit callouts on table rows and drop NOTE/NOTICE from the callout keyword set (keep them a separate 'note' class). |
| 19 | Is SF6 gas dangerous when I open this breaker? | ✅ |  |
| 20 | What's the safe digging clearance from a marked gas main? | 🟡 | hazard_class='excavation'/'gas' and applies_to_domain='gas' scope it, but the clearance distance (e.g. 'within 18-24 inches, hand-dig') is a number embedded in prose/table with no normalized clearance field, so the exact figure depends on that chunk being retrieved. Fix: extract hand-dig/tolerance-zone clearances into a structured field keyed by facility type. |
| 21 | Can I work alone on this energized secondary service? | 🟡 | applies_to_voltage='secondary' and hazard_class='energized' scope it; is_prohibition may catch 'shall not work alone' but 'must not' cases are covered while 'a second qualified employee is required' (affirmative) is not. Fix: add affirmative-requirement detection (min-crew/standby-person) as a restrictions field. |
| 22 | What voltage class is this 'medium voltage' glove good up to? | 🟡 | extract_applies_to_voltage tags 'medium_voltage' as a named tier but does NOT bind it to a numeric max-use voltage, and there is no glove-rating table field, so the numeric answer depends on retrieving the ASTM class table. Fix: structured glove ratings field (see rubber-goods scenario) and a named-tier->kV-range crosswalk. |
| 23 | Show the boxed WARNING and the note under it for the gas purge procedure. | 🟡 | The WARNING and NOTE are both matched by SAFETY_CALLOUT_RE but each is truncated at the first newline, and they are separate list entries with order not guaranteed to reflect page layout. A multi-line boxed WARNING loses continuation lines. Fix: reconstruct boxed callouts across wrapped lines and preserve a callout+note ordering/association within governing_callouts. |
| 24 | Do I need arc-rated clothing to operate this pad-mounted switch? | 🟡 | hazard_class='arc_flash' and applies_to_equipment='switch'/'switchgear' scope it, and criticality='critical' gates the abstain behavior. Gap: whether arc-rated clothing is REQUIRED for THIS operation is a policy statement that may live in a general PPE matrix not co-located with 'pad-mounted switch'; without a required_ppe/arc_flash_required field the join is not guaranteed. Fix: PPE-matrix extraction keyed by operation/equipment class. |
| 25 | What's the induced-voltage hazard when working a de-energized line parallel to a 230kV transmission line? | 🟡 | hazard_class='energized' matches 'induced voltage' and 'high_voltage' matches 230kV, so the hazard chunk is retrievable. Gap: the mitigation (equipotential grounding) may be in a different section/figure; governing_callouts won't span it and applies_to_system is a stub (echoes headers) so system-level scoping fails. Fix: real applies_to_system controlled vocab + topic_id rollup so hazard and mitigation co-retrieve. |
| 26 | Is it prohibited to remove a ground before the job is complete? | 🟡 | hazard_class won't tag 'grounding' as a hazard class (grounding is equipment, not in _HAZARD_SIGNALS), but is_prohibition/prohibitions will surface 'do not remove grounds until...' — truncated at the first line. The critical tail ('until all crew are clear and job authority notified') may be cut. Fix: de-truncate prohibition capture; optionally add a grounding hazard/rule subtag. |
| 27 | What gas concentration requires immediate evacuation? | 🟡 | hazard_class='gas' scopes it but the evacuation threshold is a number ('at or above X% LEL, evacuate') with no structured threshold field; retrieval depends on the exact chunk. If it is in a response table, rows lack callouts keyword and numeric fields. Fix: structured gas-response thresholds (min_pct/action=evacuate) with callouts on rows. |
| 28 | Can I use a metal fish tape in an energized panel? | 🟡 | hazard_class='energized' and is_prohibition can surface a 'do not use conductive/metal tools' rule, truncated at first line. If the rule is a general shop-practice statement not co-located with 'fish tape', retrieval may miss it. Fix: de-truncate; add a conductive-tools prohibition subtag so the general rule is findable from a specific-tool query. |
| 29 | What are the required approach distances table — show me the whole table for all voltages. | 🟡 | Tables are exploded into table_row records plus a table-level chunk; the parent table chunk (table_number/table_caption/table_columns/table_row_cells) can reassemble the table, but table_rows_truncated/table_rows_suppressed_count show large tables may be clipped, and table_integrity_score is a constant stub so you can't trust completeness. Fix: make table_integrity_score real and surface truncation so the bot warns 'table partial — see source'. |
| 30 | Is this NOTE actually a safety requirement or just informational? | 🟡 | SAFETY_CALLOUT_RE lumps NOTE/NOTICE with WARNING/DANGER and safety_callout is set true for them, over-flagging criticality; the bot cannot cleanly tell the user a NOTE is non-mandatory because severity isn't separated. Fix: split callout classes — keep DANGER/WARNING/CAUTION as safety, demote NOTE/NOTICE to an informational class that does NOT set safety_callout or inflate criticality. |
| 31 | What's the CAUTION about torque on the transformer bushing bolts? | 🟡 | callouts keyword 'CAUTION' + applies_to_equipment='transformer'/'bushing' can retrieve it, but if the CAUTION is multi-line it is truncated to the first line, and if it sits in a table cell it lacks the callouts keyword. Fix: de-truncate callout body and emit callouts on table rows. |
| 32 | Am I required to test for voltage before grounding on this 12kV line? | 🟡 | hazard_class='energized' scopes it and the step is retrievable, but the 'shall test before grounding' rule is a mandatory affirmative not caught by is_prohibition, so it won't surface via the prohibition filter — only via free-text search. Fix: mandatory-step detection ('shall/must ... before ...') exposed as a requirements field. |
| 33 | What if the gas detector alarms while I'm in the trench — stop work? | 🟡 | hazard_class='gas'/'excavation'/'confined_space' scope it and criticality='critical' can drive an abstain-or-cite gate, but there is no explicit stop-work/emergency-action field; the action must be retrieved from prose. Fix: add an `emergency_action`/`stop_work_trigger` enrichment extracted from 'if ... stop/evacuate/notify' patterns. |
| 34 | Which figure shows the correct rubber glove and sleeve donning? | 🟡 | has_diagram + diagram_description + diagram_category + figure_title can surface a PPE-donning figure via vision analysis, which is a real strength. Gap: PPE pictograms often have little OCR text and low_confidence_ocr may fire; diagram_category is a free string not a controlled 'ppe' value, so filtering to PPE figures isn't reliable. Fix: add a controlled diagram_category value set including 'ppe'/'safety_equipment'. |
| 35 | Can I bypass the recloser without a switching order? | 🟡 | is_prohibition may catch 'do not operate without a switching order', truncated at first line; applies_to_equipment='recloser' scopes it. If phrased as an affirmative authority requirement it's missed. Fix: affirmative-requirement/authorization detection + de-truncation. |
| 36 | What hazards are called out on figure 5-2 (the manhole layout)? | 🟡 | figure_callouts + diagram_ocr_text + hazard_class (computed over OCR) can surface hazards annotated in a figure — a real capability. Gap: OCR of small in-figure warning labels may be low-confidence (low_confidence_ocr) and _parse_figure_callouts may miss stylized labels, so hazards drawn as icons are lost. Fix: flag low-OCR figures for the bot to caveat, and improve figure-callout parsing. |
| 37 | Is 750 V considered high voltage per this manual? | 🟡 | extract_applies_to_voltage bands 750V as 'low_voltage' by IEEE convention, but the QUESTION asks the manual's own definition, which may differ; the definitions section is retrievable by text but there is no link between the computed band and the doc's stated thresholds. Fix: extract the document's own voltage-class definition table into a structured crosswalk so answers cite the manual, not the code default. |
| 38 | What PPE is prohibited — like can I wear synthetic clothing near arc-flash? | 🟡 | hazard_class='arc_flash' scopes it and is_prohibition catches 'do not wear synthetic/meltable clothing', truncated at first line so the enumerated materials list is cut. Fix: de-truncate prohibition capture to keep the full material list. |
| 39 | Do I need a fire watch when purging gas from this main? | 🟡 | hazard_class='gas' and applies_to_equipment='gas_main' scope it; the requirement text is retrievable, but 'a fire watch shall be posted' is an affirmative requirement not caught by is_prohibition and there's no requirements field, so it surfaces only via free-text/vector. Fix: mandatory-requirement extraction field. |
| 40 | What's the difference between the WARNING and DANGER for this energized capacitor work? | 🟡 | callouts keyword preserves the DANGER/WARNING labels and governing_callouts holds bodies, so the bot can distinguish them — but each body is truncated to the first line, so the substantive difference (often in later lines) is lost. Capacitor stored-charge DANGER is exactly the kind of multi-line box that truncates. Fix: de-truncate callout bodies. |
| 41 | Is this procedure still current or has it been superseded? | 🟡 | is_current_revision/supersedes_revision/document_family_id are NOT emitted by the skillset — they are only set by mark_current_revisions.py (dry-run default, absent from the heal flow), so they are null on freshly indexed records. A chatbot filter is_current_revision eq true then fails closed and returns nothing, or the bot can't confirm currency. Fix: emit revision-currency in the pipeline (or compute at query time from document_revision+effective_date) so safety currency is guaranteed. |
| 42 | Which manual and revision does this approach-distance rule come from? | ✅ |  |
| 43 | Can I energize before all clearances are released? | 🟡 | is_prohibition may catch 'do not energize until clearance released', truncated; but clearance procedures are usually mandatory affirmatives ('energization is authorized only after...') that the prohibition regex misses. criticality='critical' helps gate. Fix: conditional/mandatory rule detection + de-truncation so the full hold-off condition is returned. |
| 44 | What respiratory protection is needed around escaping gas? | 🟡 | hazard_class='gas' scopes it, but respiratory PPE (SCBA/air-purifying) is part of the missing structured required_ppe field; retrieval relies on the prose chunk. Natural-gas manuals often say respiratory protection is generally NOT required (asphyxiant, not toxic) — a nuance a lucky-chunk retrieval can get wrong. Fix: required_ppe field with hazard linkage so the respiratory answer is assembled, not guessed. |
| 45 | Show me every prohibition in the energized-work section. | 🟡 | is_prohibition + prohibitions + section_path/chapter_label filter genuinely supports enumerating do-not rules in a section — a real strength. Gaps: (1) each prohibition clause is truncated at the first line/180 chars; (2) table_row prohibitions: process_table computes is_prohibition/prohibitions on rows but rows may be suppressed/truncated; (3) affirmative mandatory rules are excluded by design. Fix: de-truncate clauses and add a parallel requirements list so a safety briefing is complete. |

## revision-currency

| # | Question | | Gap & fix (if not yes) |
|---:|---|:--:|---|
| 1 | Is this the current version of the switching procedure you just showed me? | ✅ |  |
| 2 | What's the effective date of this grounding procedure? | ✅ |  |
| 3 | Has Rev C of the live-line manual been superseded? | 🟡 | is_current_revision=false on the Rev C record answers yes/no, but there is no forward pointer to name the superseding rev. Add superseded_by_revision (and superseded_by_document_number) so the answer can say 'yes, superseded by Rev D'. |
| 4 | What changed between Rev C and Rev D of the clearance table? | ❌ | No change_summary/revision_notes/changed_values field exists. Add a per-revision change_summary (and ideally per-row change flags on table_row records) captured from the document's revision-history block. |
| 5 | Show me only the current clearance distances, not the old ones. | 🟡 | Field exists but retrieval_eligible does not encode currency, so superseded records are still eligible. App must inject filter is_current_revision=true by default; add a documented current-only retrieval policy and a UI opt-in to include history. |
| 6 | Which revision of the gas leak procedure is in effect today (2026-07-20)? | 🟡 | is_current_revision=true yields today's revision, but effective_date is Edm.String with no effective_end, so a true as-of-date comparison is not reliable. Change effective_date to Edm.DateTimeOffset and add effective_end/validity window. |
| 7 | Was the 34.5 kV minimum approach distance superseded by a newer standard? | 🟡 | is_current_revision tells if the containing doc is current, but there is no forward pointer to the replacing standard. Add superseded_by_document_number/superseded_by_revision to point crews to the new value. |
| 8 | Give me the full revision history of Document SM-4.2. | 🟡 | document_family_id groups revisions and document_revision labels them, but there is no revision_ordinal integer and effective_date is a string, so ordering (e.g., Rev 9 vs Rev 10) is unreliable. Add revision_ordinal (Int) and make effective_date a DateTimeOffset for sortable history. |
| 9 | I have Rev B in my truck — is there a newer one? | 🟡 | Can find the family's current record via document_family_id + is_current_revision=true and report its revision, but without revision_ordinal you can't robustly confirm B precedes it. Add revision_ordinal to make 'newer than B' deterministic. |
| 10 | When does the new grounding standard take effect? | 🟡 | If only the current revision is indexed, effective_date answers the current one but not a future one; there is no draft/pending status. Add a status enum (active/superseded/withdrawn/pending) and index pending revisions with future effective_date. |
| 11 | This PDF says Rev A — is that the latest revision? | 🟡 | Family current record is retrievable, but confirming Rev A is the newest needs ordering. Add revision_ordinal; also surface superseded_by_revision so the answer is unambiguous. |
| 12 | What superseded Bulletin 2019-07? | ❌ | supersedes_revision only links within the same family/number and points backward. Add superseded_by_document_number to capture renumbered/replaced documents. |
| 13 | Show me the previous version of this arc-flash table. | 🟡 | supersedes_revision holds the prior revision label; you can filter document_family_id + document_revision=that label to fetch it, but only if the prior revision is still indexed (superseded docs may be excluded). Ensure superseded revisions are retained and add a history-retention flag. |
| 14 | What's the effective date of Table 7 in the current manual? | ✅ |  |
| 15 | Which manual is authoritative right now for rubber glove testing? | ✅ |  |
| 16 | Has this procedure been withdrawn or retired, not just replaced? | ❌ | is_current_revision=false conflates 'superseded' and 'withdrawn'. Add a status enum (active/superseded/withdrawn/draft) and a withdrawn_date field. |
| 17 | Is the copy in the index up to date with the latest published revision? | 🟡 | last_indexed_at shows when it was ingested but not the source's latest published revision. Add source_published_date/source_revision from the document register and a stale flag when they diverge. |
| 18 | Give me the document number and revision so I can cite this correctly. | ✅ |  |
| 19 | Compare the old and new minimum approach distances side by side. | 🟡 | Both revisions can be fetched by family if the old one is retained, but there is no change/delta field to explain what differs. Add change_summary and, for tables, per-row changed flags; retain superseded revisions. |
| 20 | Only search documents effective after January 2024. | 🟡 | effective_date is Edm.String, so range filters ('after 2024-01') are lexical and unreliable unless strictly ISO. Convert effective_date to Edm.DateTimeOffset to enable ge/le range filters. |
| 21 | Is Rev D the revision that replaced Rev C? | ✅ |  |
| 22 | When was this manual last updated in the system? | 🟡 | last_indexed_at answers ingest time but can mislead users into thinking the document was revised then. Surface both last_indexed_at and effective_date/document_revision and label them clearly. |
| 23 | Sort the switching bulletins newest first. | 🟡 | effective_date is sortable but as a string; correct only if values are strict ISO. Make it DateTimeOffset (or add revision_ordinal) for dependable newest-first ordering. |
| 24 | Is there a pending update to the confined space procedure? | ❌ | No draft/pending status is modeled and pending revisions aren't indexed. Add status enum with 'pending' and index approved-but-not-yet-effective revisions. |
| 25 | Which revision was in effect on the incident date, 2022-05-14? | ❌ | Only effective_date (start, string) exists, no effective_end and no guarantee superseded revisions are retained. Add effective_end (DateTimeOffset), retain historical revisions, and support as-of queries. |
| 26 | Does this old printed procedure match the current standard? | 🟡 | Current revision is resolvable, but without revision_ordinal/superseded_by the system can only say the current label, not confirm the paper differs. Add forward pointer and ordinal. |
| 27 | Show me the supersession notice for this document. | ❌ | No supersession_notice text field. Capture the revision-history/supersession statement from the document into a searchable field. |
| 28 | What's the latest revision letter for the grounding manual? | 🟡 | document_family_id + is_current_revision=true returns the current record's document_revision; robust label ordering still needs revision_ordinal for numeric/mixed schemes. |
| 29 | Filter out anything that's been superseded from my results. | 🟡 | is_current_revision=true filter works but is not applied by default. Make current-only the default retrieval filter with an explicit override to include history. |
| 30 | Was the clearance value actually changed in the latest revision? | ❌ | No change/delta tracking at value or row level. Add change_summary and, for tables, changed_in_revision flags on table_row records. |
| 31 | Who approved this revision and when was it issued? | 🟡 | effective_date partially covers 'when', but there is no approved_by/issued_by/approval_date field. Add authority metadata (approved_by, issuing_org, approval_date). |
| 32 | List all documents in the SM-4 family and their status. | 🟡 | document_family_id + is_current_revision give a two-state view; a full status (superseded/withdrawn/pending) is not available. Add status enum to enrich the family listing. |
| 33 | Is this table from the current manual or an obsolete one? | ✅ |  |
| 34 | What revision supersedes the one on the page I'm reading? | 🟡 | Can find the family's current revision, but no forward pointer names the immediate successor of an arbitrary old revision. Add superseded_by_revision. |
| 35 | How many revisions has this procedure gone through? | 🟡 | Counting distinct document_revision within document_family_id works only if all historical revisions are indexed; superseded ones may be excluded. Retain history or store a revision_count on the current record. |
| 36 | Is the effective_date the approval date or the date it goes in force? | ❌ | A single effective_date conflates approval vs in-force. Split into approval_date and effective_date (in-force), both DateTimeOffset. |
| 37 | Give me the current version and tell me what's different from last year's. | 🟡 | Current version retrieval is covered; the 'what's different' narrative is not (no change_summary). Add change_summary keyed to prior revision. |
| 38 | Are these two chunks from the same revision of the manual? | ✅ |  |
| 39 | Pull the current MAD table and confirm it isn't the withdrawn one. | 🟡 | is_current_revision=true confirms current, but a withdrawn table without a replacement is indistinguishable. Add a withdrawn status so the confirmation is complete. |
| 40 | Which document number replaced the retired Standard 250? | ❌ | No superseded_by_document_number and no withdrawn/retired status. Add both to map a retired number to its replacement. |
| 41 | Is the diagram I'm about to cite from the current revision? | 🟡 | Fields are doc-level; coverage depends on is_current_revision/document_revision being propagated to diagram record_types. Verify propagation to diagram records in the skillset. |
| 42 | Warn me if I'm about to cite a superseded safety limit. | 🟡 | is_current_revision exists to build the guard, but there is no enforced rule preventing superseded safety_callout/criticality records from entering an answer. Add a policy: for safety-critical retrieval, hard-filter is_current_revision=true and flag any mixed-revision context. |
| 43 | What date range was this revision valid for? | ❌ | Only a single effective_date (start) exists. Add effective_end (populated from the successor's effective_date) as DateTimeOffset to give a validity window. |
| 44 | Was the whole manual re-issued, or only certain sections revised? | 🟡 | Revision fields are document-level; partial re-issues aren't modeled at section granularity. Add section_revision/section_effective_date on topic_id or section_path where the document issues per-section revisions. |
| 45 | Confirm this switching procedure hasn't been revised since 2020. | 🟡 | is_current_revision plus effective_date can express this, but the string date makes the 'since 2020' comparison unreliable and there is no revision_ordinal to prove nothing newer exists. Convert effective_date to DateTimeOffset and add revision_ordinal. |

## applicability-routing

| # | Question | | Gap & fix (if not yes) |
|---:|---|:--:|---|
| 1 | What's the minimum approach distance for a 4.16kV feeder? | 🟡 | applies_to_voltage exists and is filterable, but there is no controlled vocabulary/normalizer: '4.16kV' may be tagged as '4.16 kV', '4160V', '4kV class', or left empty on the actual MAD table. A user filter on '4.16kV' silently misses the row. Fix: add a voltage normalization step at index time that maps every surface form to canonical tokens (e.g. 'V_4_16KV') AND a numeric applies_to_voltage_kv Edm.Double(Collection) so both exact-match and range logic work. |
| 2 | Show me the clearance table for medium-voltage distribution (anything 5 to 15 kV). | ❌ | applies_to_voltage is a string collection — you cannot express 'ge 5 and le 15'. 'medium voltage' as a phrase won't match rows tagged only '12kV'/'4.16kV'. Fix: add numeric applies_to_voltage_kv (min/max or collection) plus a voltage_class field (low/medium/high/EHV) so range and class queries resolve. |
| 3 | Does this glove rating requirement apply to a 34.5kV circuit? | 🟡 | If the glove-class rule chunk has empty applies_to_voltage (it's a general OSHA-style table keyed by voltage internally), a filter for '34.5kV' returns nothing and the bot may wrongly say 'no requirement found'. Fix: default-scope semantics — treat empty applies_to_voltage as 'applies to all' via an applies_to_voltage_is_general boolean, and OR it into applicability filters so general rules aren't excluded. |
| 4 | What PPE do I need working on a 12kV overhead primary? | 🟡 | Fields exist but are independently, inconsistently populated per chunk; multi-facet AND filtering over-restricts (a chunk tagged voltage but not system drops out). Fix: relax to weighted boosting (scoring profile over applies_to_* tags) instead of hard AND filters, and backfill inheritance so a chunk under a '12kV Overhead' section header gets all three tags. |
| 5 | Is this switching procedure for distribution or transmission? | ✅ |  |
| 6 | Give me the gas leak classification procedure, not the electric one. | 🟡 | applies_to_domain exists and is filterable, so gas/electric split is possible IF every chunk is tagged. Real gap: mixed documents (a combined ops manual) may leave domain empty on shared safety sections, and there's no source_file->domain default. Fix: derive a document-level default domain (from document_title/doctype) and inherit to untagged chunks; add a hard exclude filter path for 'not electric'. |
| 7 | For a single-phase lateral, do I still need three-phase grounding? | 🟡 | applies_to_phase exists but is coarse; the crucial distinction (a rule that ONLY applies to three-phase vs one that applies to both) needs per-rule tagging that LLM extraction likely misses. Fix: enforce a controlled phase vocabulary {1ph,3ph,any} with an explicit 'any' default, and validate against text mentioning 'single-phase'/'three-phase'. |
| 8 | What's the approach distance for a 2.4kV secondary network? | 🟡 | 2.4kV is an uncommon surface form ('2400V', '2.4 kV', '2.4kv') → normalization gap again; and 'secondary network' equipment tagging in applies_to_equipment is free-text with no synonym set (network protector, spot network). Fix: normalize voltage + add an equipment synonym/alias map feeding applies_to_equipment and equipment_ids. |
| 9 | Which of these rules apply to padmount transformers specifically? | 🟡 | Free-text applies_to_equipment with no canonical taxonomy means 'padmount', 'pad-mounted', 'pad mount xfmr' fragment the facet; recall depends on exact tag string. Fix: build an equipment taxonomy (canonical id + aliases) at index time, store canonical in equipment_ids and keep raw in applies_to_equipment for display. |
| 10 | Show only substation-applicable arc-flash requirements. | 🟡 | applies_to_system + hazard_class both filterable, so intersection works when populated. Gap: substation vs switchyard vs 'station' vocabulary inconsistency and empty tags on general arc-flash tables. Fix: normalize system vocabulary and apply general-default OR logic. |
| 11 | Does the 4kV class cover 4.16kV, or are they treated separately? | ❌ | No class-to-value mapping exists; '4kV', '4.16kV', '4160V' are distinct opaque strings. Bot cannot reason that they're the same class. Fix: add voltage_class canonicalization mapping (4.16kV↔4kV class) plus numeric kv so the model can answer equivalence. |
| 12 | I'm on a 13.2kV feeder — is the 12kV table close enough? | ❌ | Without numeric voltage the index can't find 'nearest' or 'next conservative (higher)' class; a string filter for '13.2kV' returns nothing and the bot can't safely fall back. Fix: numeric applies_to_voltage_kv + retrieval logic that selects the next-higher governing class and states the conservative substitution explicitly. |
| 13 | For gas distribution mains, what's the safe excavation clearance? | 🟡 | Fields exist; but 'main' vs 'gas main' vs pipe-size scoping (2in, 4in) has no numeric/size field. Excavation clearance often keyed to pressure class (LP/IP/HP) — no pressure field exists. Fix: add gas-specific applies_to_pressure_class and pipe_size fields to the applicability set. |
| 14 | Which requirements are transmission-only and don't apply to my distribution work? | 🟡 | You can filter applies_to_system ne 'transmission' but a rule scoped to BOTH, or untagged, is ambiguous; there is no explicit exclusion field ('does not apply to distribution'). Fix: capture scope as inclusive tags + an applies_to_system_excludes collection so 'transmission-only' is unambiguous. |
| 15 | Does this rule say 'except for network protectors'? Does that exception apply to me? | ❌ | No exclusion/exception field; carve-outs ('except', 'does not apply to', 'excluding') live only in prose and aren't structured, so applicability filters can't honor them and may return a rule that the exception voids. Fix: extract exception clauses into applies_to_*_excludes fields and an exceptions_text field, and reflect them in routing. |
| 16 | Is grounding required on de-energized 34.5kV or only when live? | ❌ | No energization-state field exists; applies_to_* covers voltage/equipment but not operating condition, which is safety-critical here. Fix: add applies_to_condition {energized,de-energized,live-line,grounded} tags extracted from procedure context. |
| 17 | What applies to a 12kV underground residential distribution (URD) loop? | 🟡 | There's no construction-type dimension (overhead vs underground vs URD vs network); it's crammed into free-text applies_to_equipment. Fix: add applies_to_construction {overhead,underground,URD,network,submarine} as a first-class facet. |
| 18 | Show me the 500kV transmission tower clearances. | 🟡 | Same normalization gap at the high end (500kV/500 kV/EHV); and if corpus is distribution-centric these may be absent — bot must say 'out of scope' rather than return a wrong distribution table. Fix: numeric kv + voltage_class EHV, plus a coverage/authority check that returns an explicit not-in-scope message instead of nearest distribution row. |
| 19 | Which grounding requirements apply across ALL voltage levels? | ❌ | Empty applies_to_voltage is ambiguous — it could mean 'general' or 'not yet tagged'. No boolean marks a rule as intentionally voltage-agnostic. Fix: add applies_to_voltage_is_general boolean set true when text says 'all voltages'/'regardless of voltage', enabling this exact query. |
| 20 | For a 4.16kV feeder in the Northern region, are there local amendments? | ❌ | There is NO region field in the index (operationalarea is generic org metadata, not a normalized region). Region-specific amendments cannot be routed or filtered. Fix: add applies_to_region / operating_company facet populated from document metadata and in-text jurisdiction markers. |
| 21 | Is this a company-wide standard or a local operating district practice? | ❌ | No authority-scope dimension; doctype exists but doesn't encode corporate-vs-local governance level. Fix: add scope_authority {corporate,regional,local} so conflicting practices are ranked and disclosed correctly. |
| 22 | What's the phase-to-phase clearance for 34.5kV three-phase? | 🟡 | applies_to_phase can carry 3ph, but the distinction between phase-to-phase vs phase-to-ground clearance lives in table_row_semantic_key, not applicability, and 34.5kV normalization applies. Fix: normalize voltage; ensure table_row_semantic_key captures 'phase-to-phase' and is retrievable alongside the applies_to filter. |
| 23 | These rules mention '15kV class' — does my 12.47kV system qualify? | ❌ | Insulation-class grouping (5kV/15kV/25kV/35kV class) is standard utility practice but unmodeled; '15kV class' and '12.47kV' are unrelated strings here. Fix: add voltage_class with class membership so 12.47/13.2/13.8kV all map to 15kV class. |
| 24 | Filter everything to just electric transmission substation content. | 🟡 | Doable via facets when populated, but transmission and substation are separate system values and a chunk may carry only one; strict AND drops boundary content (line-to-station interface). Fix: allow multi-value applies_to_system and OR-within-facet, and normalize the vocabulary. |
| 25 | Does the 600V and below rule cover my 480V service? | ❌ | '600V and below' is a threshold, not a value; with string voltage tags the index can't test 480 ≤ 600. Fix: numeric applies_to_voltage_kv plus threshold-aware retrieval so 'X and below/above' rules match member voltages. |
| 26 | What equipment does document D-1234 apply to? | 🟡 | You can filter document_number and facet applies_to_equipment, but the facet reflects only chunks that got tagged; a document-level scope summary doesn't exist. Fix: compute and store a document-level applicability rollup (union of child applies_to_* ) on a summary record per document. |
| 27 | I have a hydraulic recloser at 12kV — which maintenance rules apply? | 🟡 | applies_to_equipment/equipment_ids can carry 'recloser' but model/type granularity (hydraulic vs electronic control) and manufacturer aren't modeled; over-broad matches result. Fix: extend equipment taxonomy with subtype/model attributes and store in equipment_ids. |
| 28 | Are these live-line (hot stick) rules valid at 34.5kV or only up to 15kV? | ❌ | Method-validity ceilings ('up to 15kV') are threshold statements unmodeled by string voltage tags; can't verify 34.5kV is within range. Fix: numeric voltage bounds (applies_to_voltage_kv_max) extracted for method chunks. |
| 29 | For gas services under 60 psig, what's the purge procedure? | ❌ | No gas pressure field; domain=gas is too coarse to separate LP/IP/HP or a <60 psig threshold. Fix: add applies_to_pressure_class and numeric pressure threshold fields for gas content. |
| 30 | Does this apply to my bare conductor, or only covered/tree wire? | 🟡 | Conductor covering type is a safety-relevant applicability axis buried in free-text; no structured attribute, so bot can't reliably distinguish bare vs covered. Fix: add applies_to_conductor_type {bare,covered,tree-wire,insulated} attribute. |
| 31 | Show only rules that apply to my exact setup: 4.16kV, overhead, three-phase, distribution. | 🟡 | Three of four axes are partly modeled (voltage, phase, system) but construction isn't, and strict 4-way AND on sparsely-tagged chunks yields near-zero recall. Fix: add construction facet, use weighted applicability scoring not hard AND, and inherit tags from section headers so all four axes are consistently present. |
| 32 | Is Table 4-2 the right table for 34.5kV or is there a separate 34.5kV variant? | 🟡 | table_variant_id and table_scope_tags exist and could encode per-voltage variants, but only if variant tagging captured voltage scope; without voltage normalization the variant selection is unreliable. Fix: populate table_scope_tags with normalized voltage tokens and expose variant selection logic keyed to applies_to_voltage. |
| 33 | Which callouts on this page are specific to underground vs overhead? | ❌ | callouts/governing_callouts are text collections with no applicability tags of their own; construction scoping of an individual DANGER note isn't captured. Fix: attach applicability tags (construction/voltage) to callout objects, or inherit chunk-level applies_to_construction to callouts. |
| 34 | Does this substation grounding standard apply to my pole-top distribution transformer? | 🟡 | Fields support detecting substation vs distribution mismatch when populated, but nothing forces the bot to REFUSE/warn on mismatch; retrieval may still surface and present the substation rule. Fix: add a routing guard that compares user scope to top-hit applies_to_* and emits an explicit applicability-mismatch warning. |
| 35 | For 25kV, are the clearances the same as 34.5kV or 15kV class? | ❌ | Comparing across classes needs numeric/class structure the index lacks; string tags can't order 15<25<34.5. Fix: numeric kv + voltage_class enable ordered comparison and correct class assignment (25kV → 25/28kV class). |
| 36 | Only show me rules effective for the current revision that apply to 12kV. | ✅ |  |
| 37 | Does this rule cover both electric AND gas crews at a joint-trench site? | 🟡 | applies_to_domain is a collection so it CAN hold both, but extraction likely assigns a single domain per chunk; joint-occupancy/shared-trench rules get mis-tagged to one domain. Fix: allow and validate multi-domain tagging and add a 'joint/shared' domain marker. |
| 38 | What voltage does this figure's clearance diagram assume? | 🟡 | applies_to_* live on chunks; figure/diagram records may not inherit them, so a diagram's governing voltage is unknown unless in diagram_ocr_text. Fix: propagate parent chunk applies_to_* to child figure/table/row records via topic_id/parent_id at index time. |
| 39 | Route me to the transmission version of the switching order, not the distribution one with the same title. | 🟡 | document_family_id groups variants and applies_to_system can distinguish them, but only if both are populated consistently; title collision plus missing system tag causes wrong pick. Fix: ensure applies_to_system is document-level and tie variant selection to document_family_id + system scope. |
| 40 | Is this 4.16kV or 13.8kV? The manual just says 'primary voltage'. | ❌ | Relative terms ('primary','secondary','distribution voltage') aren't mapped to concrete values; applies_to_voltage would be empty or literally 'primary'. Fix: capture a voltage_reference_term field and, where a document declares its nominal primary, resolve/annotate it; otherwise flag as under-specified. |
| 41 | Give me only the rules applicable to network protectors in a spot network. | 🟡 | Niche equipment/construction terms fragment across free-text tags with no alias set (network protector/NWP, spot network/secondary network). Fix: equipment + construction taxonomies with aliases feeding applies_to_equipment/applies_to_construction. |
| 42 | For 4.16kV work, does the 5kV insulation-class glove rule apply? | ❌ | No mapping between system voltage and rubber-goods insulation class (Class 0-4 / max-use voltage); the bot can't connect 4.16kV to the 5kV class table. Fix: add a rubber_goods_class / max_use_voltage_kv mapping table and link via numeric voltage. |
| 43 | Which of these apply to de-energized AND grounded 12kV vs just de-energized? | ❌ | No operating-condition dimension exists to separate 'de-energized' from 'de-energized+grounded'; safety-critical distinction lost. Fix: add applies_to_condition tags and combine with applies_to_voltage in retrieval. |
| 44 | Does this apply to my bucket truck / aerial lift at 34.5kV, or only to structure-mounted work? | 🟡 | Work-platform type (bucket, insulated aerial device, climbing) is a real applicability axis for minimum approach; only loosely in applies_to_equipment. Fix: add applies_to_work_method and ensure aerial-device insulation rating is captured. |
| 45 | Show only gas-electric common (shared) safety rules that apply regardless of domain. | ❌ | Empty applies_to_domain conflates 'general' with 'untagged'; can't isolate intentionally shared rules. Fix: applies_to_domain_is_general boolean (mirror of the voltage-general fix) so shared rules are explicitly retrievable. |
| 46 | For a 12kV feeder, which figures and tables are in-scope so I can pull the whole clearance package? | 🟡 | topic_id groups cross-type content, but if applies_to_voltage isn't inherited onto table/figure/row records the voltage filter fragments the package. Fix: inherit applies_to_* across the topic_id group so a single voltage filter returns the full text+table+figure set for citation. |

## cross-reference

| # | Question | | Gap & fix (if not yes) |
|---:|---|:--:|---|
| 1 | Walk me through the switching procedure in Section 4.2 and include Figure 4-3 that it points to. | 🟡 | figures_referenced_normalized carries 'Figure 4-3' but no field pre-links it to the target figure record's id, so a single vector pass returns the text only. Add index-time resolved_figure_ids: Collection(Edm.String) that joins figures_referenced_normalized to the matching figure_number within the same document_family_id; retriever fetches those ids directly. |
| 2 | The grounding step says 'see Table 3-2 for minimum approach distances' — what are the actual numbers? | 🟡 | tables_referenced_normalized='Table 3-2' can filter on table_number, but (a) no resolved_table_ids join field and (b) the table is exploded into table_row records that must be gathered via tbl_row_parent_id. Add resolved_table_ids plus retriever logic to pull all rows sharing table_cluster_id/tbl_parent_id so the whole clearance table is returned, not one row. |
| 3 | Per Figure 6, where exactly do I clamp the personal protective ground? | 🟡 | figure_number is filterable and diagram fields are populated, so the figure is findable, but if two documents both contain a 'Figure 6' the number alone is ambiguous. Constrain the figure lookup by document_family_id/document_number carried from the citing chunk; add a normalized figure key that includes the doc scope. |
| 4 | Follow every cross-reference in the live-line rubber glove inspection procedure and give me the complete set of linked figures, tables and sections. | ❌ | The index stores the reference strings but performs no resolution and single-pass RAG won't chase them. Add resolved_figure_ids/resolved_table_ids/resolved_section_topic_ids at index time, and an orchestration step that unions the citing chunk's resolved-id fields into a second retrieval batch. |
| 5 | It says 'refer to Section 5 for required PPE' — pull Section 5. | ❌ | sections_referenced holds 'Section 5' but there is no canonical section_number field on target chunks to join against — section_path/header_* are free text and chapter_number is chapter-level, not section-level. Add section_number (+ normalized) to every chunk and a resolved_section_topic_ids join so 'Section 5' maps to a topic_id. |
| 6 | This step references 'the preceding step' — what was the step before it? | ✅ |  |
| 7 | The paragraph ends with 'continued on the following page' — give me the rest. | ❌ | chunk_next_id/chunk_prev_id are empty stubs, so continuation cannot be followed. Populate them at index time from layout_ordinal ordering within topic_id/section_path so 'continued' references resolve to the adjacent chunk. |
| 8 | 'See Note 3 below' — what does Note 3 say? | 🟡 | footnotes is a retrievable collection on the record, so if the note lives on the same chunk it comes along, but there is no footnote_number key to resolve 'Note 3' when the note is on a sibling chunk. Add footnote_number-keyed entries (or a note registry per topic_id) so numbered note references resolve. |
| 9 | The clearance table cell says 'See Appendix B' — what's in Appendix B? | ❌ | References inside table_row_cells are not parsed into sections_referenced, and appendices have no dedicated locator field. Extract in-cell references during row processing and add an appendix_label to appendix chunks so 'Appendix B' resolves; flag if the appendix is out-of-corpus. |
| 10 | Procedure 7.1 says 'first complete the isolation procedure in 6.4' — pull that prerequisite procedure too. | 🟡 | sections_referenced may capture '6.4' but there is no link to the target procedure_id nor a prerequisite flag. Add resolved_prerequisite_procedure_ids and a reference-role tag (prerequisite vs see-also) so the chatbot surfaces prerequisites before steps. |
| 11 | What clearance does this manual give, and does it reference the current revision of the clearance table or a superseded one? | 🟡 | is_current_revision/supersedes_revision/document_family_id exist, but reference resolution does no revision awareness, so a 'Table 3-2' join could land on a superseded record. Make resolved_table_ids prefer is_current_revision=true within the family and expose a stale-reference warning. |
| 12 | The step says 'as shown in the diagram at right' — show me that diagram. | ❌ | No number to key on, and figure_step_linked/figure_linkage_confidence only cover procedure-step linkage, not narrative 'at right/above' refs. Add a same-page positional linker using physical_pdf_page + layout_ordinal/bbox to attach the nearest figure, stored as resolved_figure_ids with a positional-confidence score. |
| 13 | 'See Figures 4-1 through 4-4' — give me all four figures. | 🟡 | figures_referenced_normalized likely stores the literal 'Figures 4-1 through 4-4' or a partial list, not an expanded set. Normalize ranges into discrete figure keys at index time and resolve each to a figure record id. |
| 14 | Table 5-1 is marked 'continued' — did I get the whole table or just the first page of it? | 🟡 | table_split_index/table_split_count/table_cluster_id/table_rows_truncated exist, so completeness is detectable, but resolving 'Table 5-1' must gather every split — not guaranteed by a single hit. Retriever must union all records sharing table_cluster_id and check table_rows_truncated before answering. |
| 15 | Who else in the manual points to Table 3-2? I want every procedure that relies on it. | ✅ |  |
| 16 | Show me everything that references Figure 4-3 and the figure itself side by side. | 🟡 | Both directions are individually supported by filters, but there is no single stored link tying the figure record to its citer ids. Optionally add resolved_citer_ids on figure records to avoid a runtime reverse scan; otherwise orchestrate two filter queries. |
| 17 | The warning box says 'do not proceed without the clearances in Table 2 — see Section 3.1'. Give me the warning, the table and the section. | 🟡 | safety_callout/callouts/governing_callouts are captured, but references inside callout text are not extracted into the *_referenced fields, so the table/section links are lost. Run reference extraction over callout text and populate figures/tables/sections_referenced from it, preserving the safety context. |
| 18 | 'Per document GO-25, Rule 12' — that's a different manual. Pull Rule 12 from GO-25. | ❌ | document_number/document_family_id exist but references to other documents are not parsed or resolved, and rule-level locators aren't indexed. Add a cross_document_refs field (parsed doc number + locator) and resolve to the target document's current-revision chunk; flag if GO-25 is not in the corpus. |
| 19 | It cites 'the National Electrical Safety Code, Rule 441' — do we have that, or is it external? | ❌ | No field marks whether a reference target exists in the index, so the model may fabricate NESC content. Add ref_resolution_status (resolved/unresolved/external) computed at index time so the chatbot can cite the external standard by name and explicitly say it is not in the corpus. |
| 20 | Follow the chain: the tailboard step points to Section 8, which points to Table 8-3, which has a note pointing to Figure 8-1. Give me the whole chain. | ❌ | Even with per-field reference strings, nothing chains resolutions and single-pass retrieval stops at hop one. Add resolved_*_ids so each hop is a direct id fetch, and an agentic loop that expands newly retrieved chunks' references up to a bounded depth. |
| 21 | 'See page 44 for the phase diagram' — take me to page 44's diagram. | 🟡 | pages_referenced captures '44' and printed_page_label/physical_pdf_page are filterable, but there is no join and printed vs physical page ambiguity is unresolved. Add resolved_page_targets that map pages_referenced to physical_pdf_page via the printed→physical mapping, honoring printed_page_label_is_synthetic. |
| 22 | The step references 'Figure 4.3' but the caption prints it as 'FIG. 4-3'. Will the reference still resolve? | 🟡 | figures_referenced_normalized is normalized, but the join succeeds only if figure_number is normalized identically; 'Figure 4.3' vs 'FIG. 4-3' can mismatch. Guarantee both citer refs and target figure_number/table_number pass through the same canonicalizer, and store a shared normalized_ref_key on both sides for the join. |
| 23 | 'See the table on the previous page' — which table is that? | ❌ | No relative-page resolver and chunk_prev_id is empty. Add logic to resolve 'previous/next page' references using the citing chunk's physical_pdf_page offset filtered to record_type=table, stored as a resolved target id. |
| 24 | Give me the minimum approach distance for 230 kV, and if the procedure text cites a different table than the MAD table, tell me which one governs. | 🟡 | applies_to_voltage + table rows can answer the value, and governing_callouts exists, but there is no linkage marking which referenced table is authoritative for MAD. Tag reference roles (governing vs informational) during extraction so conflicting cited tables can be disambiguated. |
| 25 | The procedure says 'repeat steps 3 through 6' — list those exact steps. | ✅ |  |
| 26 | 'As required by the switching order form (see Figure 2)' — show the form and the step that references it, cited to page. | 🟡 | figure resolution + printed_page_label/physical_pdf_page support citation, but without resolved_figure_ids the co-fetch is not guaranteed. Add the join field; citation fields are already present once the target record is retrieved. |
| 27 | Which figures does the whole 'grounding' section reference, across every chunk in that section? | 🟡 | topic_id groups the section and figures_referenced_normalized is facetable, so a facet over the topic returns the set, but resolving each to the actual figure record still needs the missing resolved_figure_ids join. Add it to make the aggregated list directly retrievable. |
| 28 | 'Refer to the manufacturer's instructions supplied with the tester' — do we have those? | ❌ | No ref_resolution_status field, so the bot can't cleanly say the manufacturer doc isn't in the corpus. Add the external/unresolved marker (see NESC scenario) so it answers honestly. |
| 29 | The step cites both 'Figure 5' and 'Table 5-2' — make sure I get both, not just whichever the search ranked higher. | 🟡 | Relevance ranking may surface only one referenced artifact. Deterministic resolved_figure_ids + resolved_table_ids co-fetch removes reliance on ranking so both the figure and the table always accompany the citing step. |
| 30 | 'See 4.2.1' vs 'see 4.2' — resolve to the exact subsection, not the whole section. | ❌ | Without a canonical multi-level section_number on chunks, '4.2.1' can't be distinguished from '4.2'. Add hierarchical section_number (normalized, dotted) per chunk so subsection references resolve to the right header_3-level content. |
| 31 | The note says 'except as modified by Bulletin 22-07' — has this rule been amended? | 🟡 | supersedes_revision/is_current_revision/document_family_id support supersession, but references to amending bulletins aren't parsed or linked. Add amendment reference extraction linking to the amending document's chunks and surface the amended rule as governing. |
| 32 | Show me Figure 4-3 and confirm which procedure step actually uses it. | ✅ |  |
| 33 | 'See diagram below' but the diagram is actually two pages later — still find it. | ❌ | Positional references aren't resolved at all and naive adjacency would miss a target two pages away. Add a positional resolver that searches forward within the same topic_id for the next figure record, with a confidence score, stored as resolved_figure_ids. |
| 34 | Pull the PPE table referenced by the confined-space entry procedure and cite the document number and revision. | 🟡 | Citation fields (document_number, document_revision, printed_page_label) are present on the target once retrieved; the gap is the missing resolved_table_ids join to guarantee the referenced table is fetched. Add it; citation then completes. |
| 35 | 'See related procedures' at the end of the section — list them. | 🟡 | sections_referenced may capture the numbers but without reference-role tagging they're indistinguishable from prerequisites, and there's no join to procedure_id. Add reference_role and resolved_prerequisite/related_procedure_ids so see-also links are listed as optional related reading. |
| 36 | The reference says 'Table 3-2 (metric)' and there's also 'Table 3-2 (US)' — give me the right variant. | 🟡 | table_variant_id/table_scope_tags exist to distinguish variants, but the reference resolver doesn't carry the variant qualifier from the citing text. Parse parenthetical qualifiers into the resolution and match on table_variant_id/table_scope_tags. |
| 37 | The step cites Figure 4-3 but that figure's OCR is low-confidence — warn me before I rely on it. | ✅ |  |
| 38 | 'Comply with the clearances in Section 3 and the isolation steps in Section 4' — I need both sections fully. | ❌ | No canonical section_number join means neither 'Section 3' nor 'Section 4' resolves reliably, and returning a full section requires gathering all chunks of a topic_id. Add section_number + resolved_section_topic_ids and gather-by-topic_id retrieval. |
| 39 | This form references 'Item 5 in the checklist above' — what is item 5? | 🟡 | List items aren't individually indexed (unlike procedure steps), so 'Item 5' can't be addressed if it lives on another chunk. Where the list is a procedure, procedure_step_order covers it; otherwise add item-level indexing or resolve via chunk_prev_id (once populated). |
| 40 | Two different manuals both say 'see Table 1' — make sure the reference resolves within the SAME manual, not the other one. | 🟡 | document_family_id exists to scope the join, but resolution logic must explicitly filter the referenced-number match to the citing chunk's family. Bake document_family_id scoping into the resolved_*_ids computation so 'Table 1' never leaks across manuals. |
| 41 | Give me the switching procedure with every referenced figure inlined at the step that mentions it, in order. | 🟡 | figures_referenced_normalized is per-chunk/step and procedure_step_order gives order, but without resolved_figure_ids the inline figure fetch isn't guaranteed. Add the join keyed per step so each step's figure can be placed inline. |
| 42 | The clearance note says 'whichever is greater, see Table 3-2 and Table 3-3' — pull both and compare. | 🟡 | Both table numbers may be in tables_referenced_normalized, but each must be resolved and fully assembled (all rows/splits). Add resolved_table_ids and gather-all-rows so the bot can present both and apply the 'greater of' rule. |
| 43 | 'Refer to the definitions in Section 1 for terms used here' — resolve a term I don't know, like 'qualified worker'. | 🟡 | Section reference resolution is unreliable (no section_number join) and definitions aren't individually keyed. Add section_number resolution plus, ideally, a defined-term index so 'qualified worker' maps to its definition chunk within the cited section. |
| 44 | When I ask about phase-to-ground clearance, automatically bring in whatever figure and table the governing procedure references, without me naming them. | ❌ | Nothing auto-expands a retrieved chunk's references; the strings sit unused. With resolved_figure_ids/resolved_table_ids present, add a retrieval post-step that always co-fetches the governing chunk's referenced artifacts so completeness doesn't depend on the user naming them. |
| 45 | The procedure says 'obtain a switching order per the process in Section 2' — is that a hard prerequisite or just informational, and pull it either way. | 🟡 | criticality/is_prohibition describe the chunk, but references carry no role classification, so 'per the process in Section 2' isn't marked mandatory. Add reference_role during extraction (and resolve the section) so the bot states whether it's a required prerequisite. |

## comparison-troubleshoot

| # | Question | | Gap & fix (if not yes) |
|---:|---|:--:|---|
| 1 | What's the difference between Class 2 and Class 3 rubber gloves? | 🟡 | Each class likely lives in a different table_row or chunk; a single vector query on the phrase may return one class's row and miss the other, and there is no comparison-join. Fix: query decomposition (retrieve per-entity using applies_to_voltage/table_row_semantic_key) then LLM aligns; add a retrieval mode that fetches all sibling rows sharing table_number so the whole class table comes back together. |
| 2 | Difference between grounding and bonding in an equipotential zone setup? | 🟡 | 'grounding' and 'bonding' definitions may sit in different section_path branches; top-k vector search can favor whichever term is denser and drop the other. Fix: decompose into two sub-queries and merge; use topic_id to pull the section group that discusses both together. |
| 3 | How does minimum approach distance for 13kV compare to 34.5kV? | 🟡 | MAD tables are stored as table_row records keyed by voltage (table_row_semantic_key/value, applies_to_voltage). One query may surface only one voltage row. Fix: retrieve all rows of the MAD table_number together (sibling-row expansion) so both voltage rows are present for the LLM to compare and cite the same table/page. |
| 4 | What's the difference between a temporary protective ground and a permanent ground? | 🟡 | No comparison structure; relies on both appearing in retrieved chunks. Fix: entity-faceted retrieval keyed on applies_to_equipment/system, then synthesize; ensure both definitions are eligible (not filtered by retrieval_eligible). |
| 5 | Compare a Grade 1, Grade 2, and Grade 3 gas leak — which requires immediate action? | 🟡 | Grade definitions are typically one classification table; criticality/hazard_class may tag chunks but grades aren't modeled as an ordered enum. Fix: pull the full grade table via sibling-row expansion; add a normalized severity/urgency field so the model can rank rather than guess ordering. |
| 6 | Difference between working the line de-energized versus energized for this repair? | 🟡 | procedure_branch_label can tag 'energized'/'de-energized' branches, but chunk_prev_id/chunk_next_id are empty stubs so a multi-step branch can't be walked end to end. Fix: populate chunk_prev/next and group by procedure_id+procedure_branch_label so each full branch is retrievable as a unit. |
| 7 | What's the difference between a recloser and a sectionalizer? | 🟡 | Definitions may be in separate sections/manuals; single query risks one-sided retrieval. Fix: sub-query per device using applies_to_equipment facet and merge; verify both are is_current_revision. |
| 8 | Difference between phase-to-ground and phase-to-phase minimum clearance? | 🟡 | Both often in the same MAD/clearance table but as different columns/rows; row-level chunking can split them. Fix: sibling-row + full-column expansion on table_number so both categories are co-present and cite one table/page. |
| 9 | If the combustible gas indicator reads 20% LEL, what do I do? | 🟡 | Thresholds live as prose or table_row text; there is no numeric-range field, so retrieval is lexical/semantic on '20% LEL' and the LLM must interpret which band it falls in. Fix: extract structured threshold ranges (min/max + action) so the reading can be bucketed deterministically, then cite the source row. |
| 10 | If a megger insulation reading is below 1 megohm, is the cable good? | 🟡 | Acceptance limit is a table_row_semantic_key/value or prose; no numeric comparison logic in the index, and the correct table must be disambiguated by cable class. Fix: structured min/max acceptance fields plus applies_to filters to select the right criteria table before comparing. |
| 11 | The recloser keeps locking out — what should I check first? | 🟡 | If the manual has a troubleshooting table it maps to table_columns (Symptom/Cause/Action) + table_row_search_text, which retrieves well by symptom; but there is no symptom/cause/remedy semantic typing, and if guidance is narrative prose it's just a chunk. Fix: detect troubleshooting tables and tag record_subtype='troubleshooting' with typed symptom/cause/action fields. |
| 12 | Gas odor reported but the CGI shows zero — what next? | 🟡 | This is an if/then edge case usually in prose; retrievable as a chunk but not modeled as a branch, and answer completeness depends on the chunk containing the full contingency. Fix: procedure_branch_label extraction for negative/exception branches plus chunk_prev/next linking so the follow-on steps come with it. |
| 13 | Cathodic protection reading is below -0.85V — what does that mean and what do I do? | 🟡 | The -0.85V criterion is likely a table_row/prose value; no numeric-polarity reasoning in index (sign/threshold), and the LLM must handle the negative-voltage comparison. Fix: structured criterion field (operator + value + unit) so under/over-protection is decided deterministically and cited. |
| 14 | Ground resistance tested at 30 ohms — is that acceptable or do I add rods? | 🟡 | Spec limit and remediation may be in different chunks (a table value vs a prose rule); one query may return only the number without the 'if exceeded, add rods' action. Fix: link value rows to their governing action via topic_id/section grouping and co-retrieve; add numeric limit field. |
| 15 | When should I use rubber gloves versus working barehand (glove vs barehand method)? | 🟡 | Method-selection criteria are conditional prose; procedure_branch_label may capture 'glove method'/'barehand method' but the selection rule tying condition→method isn't a typed field. Fix: extract decision criteria as conditions and link to each branch; ensure both branches retrievable together. |
| 16 | When should I call for a written clearance instead of just a tag? | 🟡 | Trigger conditions are prose/if-then; retrievable as a chunk but completeness depends on all conditions being in one chunk (chunk boundaries can split a list). Fix: chunk_prev/next linking so a split condition list reassembles; consider a conditional-rule extraction. |
| 17 | If a wire is down, when is it ever safe to approach it? | 🟡 | is_prohibition/prohibitions capture the 'never approach' rule well and safety-boost surfaces it; but the conditional exceptions ('only after confirmed de-energized and grounded') may be in a separate chunk. Fix: link prohibition chunks to their exception/condition chunks via topic_id so the full rule + caveat is cited together. |
| 18 | If the test light doesn't glow, does that guarantee the line is dead? | 🟡 | The caveat ('a non-glowing tester does not prove de-energized; test the tester') is a safety_callout/warning often adjacent to the procedure step; retrieval may return the step without the warning. Fix: attach governing_callouts/safety_callout to the step via figure/step linkage and co-retrieve warnings with steps. |
| 19 | What if the pressure exceeds MAOP during purging? | 🟡 | MAOP value and the over-pressure action may be in different records (spec table vs emergency procedure); no numeric trigger link. Fix: co-retrieve limit + contingency via topic_id; structured limit field with linked action. |
| 20 | Two of my manuals list different minimum approach distances for 4kV — which one governs? | 🟡 | is_current_revision + supersedes_revision + document_family_id + effective_date let the system prefer the current doc, but if retrieval_eligible filters out the superseded row the model can't explain WHY they differ. Fix: keep superseded revisions retrievable-but-deprioritized and surface effective_date/document_revision so the answer states which governs and why. |
| 21 | Did the MAD for 500kV change between the 2019 and current revision? | 🟡 | document_family_id groups revisions and supersedes_revision chains them, but there is no cross-revision value-diff; and the old revision may be excluded by is_current_revision filters at query time. Fix: allow explicit revision-scoped retrieval and align rows by table_row_semantic_key across revisions to compute the diff. |
| 22 | What's the difference in required PPE for 480V versus 15kV work? | 🟡 | PPE tables are voltage-keyed table_rows (applies_to_voltage); a single query tends to return one tier. Fix: retrieve per-voltage rows (facet on applies_to_voltage) and co-present; sibling-row expansion for the PPE table. |
| 23 | Transformer is humming loudly and buzzing — what are the likely causes? | 🟡 | Only covered if a troubleshooting table exists (maps to table_columns/table_row_search_text and retrieves by symptom); narrative diagnostics are plain chunks with no symptom typing. Fix: troubleshooting-table detection + typed symptom/cause fields; otherwise answer risks being incomplete or uncited. |
| 24 | Voltage regulator isn't stepping — walk me through diagnosis. | 🟡 | Diagnostic trees need branch + step ordering; procedure_branch_label + procedure_step_order exist but chunk_prev/next stubs are empty so multi-node trees can't be traversed reliably. Fix: populate sequential links and group branches by procedure_id so the full tree returns. |
| 25 | Padmount transformer smells like burning — is it safe to open? | 🟡 | hazard_class/safety_callout/is_prohibition surface the 'do not open' rule strongly, but the conditional ('if burning odor/smoke, do not operate, isolate first') may be split from the operating steps. Fix: link callouts to the relevant procedure via governing_callouts and co-retrieve. |
| 26 | Phase voltages read unbalanced — one leg high — what does that indicate? | ❌ | No pattern/relationship reasoning across readings and no symptom typing; unless a manual has an explicit 'unbalanced phases' troubleshooting entry, retrieval returns generic voltage text. Fix: add troubleshooting/diagnostic record typing and, ideally, symptom-pattern indexing; otherwise flag as not confidently answerable. |
| 27 | If soil temperature is below freezing, can I still do the CP survey? | 🟡 | Environmental conditionals are prose caveats; retrievable if in the chunk but not modeled as a condition, so easy to miss if the caveat is in a note/footnote. Fix: footnotes field is captured — ensure footnotes co-retrieve with the parent step; extract environmental preconditions. |
| 28 | When do I need a standby/second person versus working alone? | 🟡 | Rule is conditional prose, often a list of triggering conditions that may be split across chunk boundaries. Fix: chunk_prev/next linking to reassemble split condition lists; ensure the governing rule is cited with its conditions. |
| 29 | If arc-flash incident energy exceeds my suit's cal rating, what are my options? | 🟡 | Incident-energy vs cal-rating comparison and the resulting options are conditional prose; no numeric compare in index. Fix: structured energy/rating fields plus linked contingency; co-retrieve the limit and the action. |
| 30 | What's the difference between a hot stick and a shotgun (grip-all) stick? | 🟡 | Tool descriptions may be in different figure/diagram records (diagram_description/figure_title) and prose; single query risks one-sided. Fix: sub-query per tool via figure_ref/applies_to_equipment and merge, citing each figure/page. |
| 31 | Compare glove air-test frequency to sleeve inspection frequency. | 🟡 | Intervals are table_row/prose values in different rows; one query may return only gloves. Fix: sibling-row expansion or two targeted lookups keyed on table_row_semantic_key ('glove test interval','sleeve inspection interval') then compare and cite. |
| 32 | If corrosion pit depth exceeds the wall-loss limit, do I repair or replace? | 🟡 | Limit table and the repair/replace decision are typically separate; no numeric trigger and possibly two branches. Fix: co-retrieve limit + both remediation branches via topic_id; structured limit field with operator. |
| 33 | When should I purge a gas main with nitrogen versus direct purge? | 🟡 | Two method branches selected by pipe size/pressure conditions; procedure_branch_label may tag methods but the selection condition isn't typed. Fix: extract decision criteria linked to branches and co-retrieve both branches with their conditions. |
| 34 | Difference between an EPZ (equipotential zone) ground and single-point grounding? | 🟡 | Two concepts likely in adjacent sections; retrieval may favor one. Fix: topic_id grouping to pull the section that discusses both; sub-query merge as fallback. |
| 35 | If I find the grounding cluster damaged mid-job, what do I do? | 🟡 | Mid-procedure contingencies are branch/exception content; procedure_branch_label helps but empty chunk_prev/next means the model can't reliably locate where in the sequence to resume. Fix: populate sequential links and branch grouping. |
| 36 | Customer reports flickering lights — what's the diagnostic sequence? | 🟡 | Covered only if an explicit troubleshooting procedure/table exists; retrieval by symptom text works via table_row_search_text/chunk, but step ordering across chunks is fragile with empty next/prev links. Fix: sequential linking + troubleshooting record typing. |
| 37 | If the reading on one CT leg is high, is it a wiring or a load issue? | ❌ | No diagnostic/symptom typing and no reasoning over reading relationships; unless a specific troubleshooting entry exists the index returns generic CT text. Fix: add typed troubleshooting entries; flag such pattern-inference questions as low-confidence and cite only if an explicit entry is found. |
| 38 | When must I bond across a cut versus when is a single bond enough? | 🟡 | Conditional rule in prose, potentially with a figure. Fix: co-retrieve the governing_callouts/prohibition with the step and the referenced figure via figures_referenced_normalized. |
| 39 | What's the difference between a Grade 1 leak response and a hazardous-condition response? | 🟡 | Two protocols may span different sections/manuals; single query risks partial retrieval. Fix: decompose and merge; use topic_id/section_path to gather each protocol fully and cite both. |
| 40 | If the OCR-extracted torque value looks wrong, how do I know whether to trust it? | ✅ |  |
| 41 | Which is more hazardous: an oil-filled switch failure or a gas-insulated switch failure? | 🟡 | hazard_class/criticality tag chunks per equipment (applies_to_equipment), enabling a rough comparison, but severity isn't an ordered scale and both equipment's hazard chunks must be co-retrieved. Fix: normalized ordered severity field + per-entity retrieval so a defensible ranking with citations is possible. |
| 42 | If pressure test fails to hold, what's the next step versus if it holds? | 🟡 | Pass/fail branches map to procedure_branch_label but empty chunk_prev/next and split chunks make it easy to return only one branch. Fix: group by procedure_id + branch label and link sequentially so both outcomes are returned and cited. |
| 43 | Difference between cover-up rubber goods and line hose — when do I use each? | 🟡 | Products described in separate rows/figures; selection criteria conditional. Fix: sub-query per product (applies_to_equipment/figure_ref) and merge; co-retrieve usage conditions. |
| 44 | If the regulator tap position and the voltage output disagree, which do I trust? | ❌ | No modeling of indicator-disagreement diagnostics or reading relationships; only answerable if a specific manual passage exists, otherwise generic retrieval. Fix: troubleshooting record typing; explicitly return low-confidence with a 'consult supervisor' fallback when no cited rule is found. |
| 45 | When can I re-energize versus when must I patrol the line first after a lockout? | 🟡 | Conditional rule likely a prohibition + condition list that may span chunks. Fix: is_prohibition/prohibitions surface the 'do not reclose until patrolled' rule; link to its condition chunk via topic_id and reassemble split lists with chunk_prev/next. |
| 46 | Difference between the old (superseded) grounding procedure and the current one — what changed? | 🟡 | document_family_id/supersedes_revision link revisions but there is no step-level diff and superseded content may be filtered by is_current_revision/retrieval_eligible. Fix: revision-scoped retrieval that aligns procedure_step_order across revisions to produce a cited change summary. |
| 47 | If I smell gas inside a building versus outside, does the response differ? | 🟡 | Two location-conditioned branches; procedure_branch_label may tag them but co-retrieval of both isn't guaranteed by a single query. Fix: branch grouping + decompose query into indoor/outdoor sub-queries and present both with citations. |

## meta-navigation-nlq

| # | Question | | Gap & fix (if not yes) |
|---:|---|:--:|---|
| 1 | Which chapter covers grounding and bonding? | 🟡 | chapter_label/chapter_number/header_1 exist and are facetable, but only on text records and per-chunk; there is no per-document chapter directory, so retrieval returns whichever sub-chunk ranks and may name a sub-section rather than the governing chapter. Fix: emit a per-document structure/TOC record (record_type='structure') listing chapter_number→title→page range, or drive a chapter_label facet scoped to source_file to pick the dominant chapter. |
| 2 | What page is Figure 18-117 on? | ✅ |  |
| 3 | What page is Table 18-3 on? | ✅ |  |
| 4 | 12kV xfmr PM checklist | 🟡 | 'xfmr' and 'PM' are not in any synonym map (none exist in index.json) so BM25 misses them; ada-002 vector may not reliably expand utility shorthand. applies_to_voltage/equipment_ids/procedure_title could scope it but only if the tokens resolve. Fix: add a synonymMap (xfmr→transformer, PM→preventive maintenance) and query-time abbreviation expansion; filter applies_to_voltage='12kV'. |
| 5 | What does MAD stand for? | 🟡 | record_subtype='glossary' can hold the definition, but 'MAD' is a noisy 3-letter token in BM25 and there is no acronym dictionary or synonym map, so retrieval is unreliable. Fix: add an acronym/abbreviation glossary field (or synonymMap MAD→minimum approach distance) and bias record_subtype='glossary'. |
| 6 | What's the minimum approach distance for 12kV? | 🟡 | table_row + table_row_semantic_key/value + applies_to_voltage can hit the exact cell, but the query often arrives as 'MAD' (unexpanded) and the phase/exposure column choice (phase-to-phase vs phase-to-ground) is ambiguous. Fix: synonym expansion for MAD; ensure the MAD table is row-split with voltage and exposure as semantic keys. |
| 7 | OH vs URD secondary requirements | ❌ | 'OH' and 'URD' are not expanded anywhere; BM25 treats 'OH' as near-stopword and vector match on bare initialisms is weak, so retrieval largely fails. Fix: synonymMap OH→overhead, URD→underground residential distribution; consider applies_to_domain/applies_to_system tagging to filter. |
| 8 | PCB disposal requirements | 🟡 | No disambiguation signal; ada-002 may conflate PCB senses, and there is no domain synonym constraining it to the utility meaning. Fix: domain-scoped synonym/disambiguation (PCB→polychlorinated biphenyl within applies_to_domain gas/electric), and route via applies_to_domain. |
| 9 | trasnformer inspection procedure | 🟡 | The query body sets queryLanguage=en-us but does NOT set speller=lexicon, so there is no query spell-correction; only ada-002 vector similarity absorbs the typo, which is inconsistent for domain nouns. Fix: add speller='lexicon' to the search request and/or a fuzzy BM25 fallback. |
| 10 | sectionaliser settings | 🟡 | US corpus spells 'sectionalizer'; no synonym/variant map, no speller param, so BM25 misses and vector is the only bridge. Fix: synonymMap for -ise/-ize and other US/UK variants; enable speller. |
| 11 | What's the MAD for 12kV and what PPE do I need? | 🟡 | A single embedded query blends both intents and dilutes ranking; the index has the pieces (table_row for MAD, safety_callout/text for PPE) but there is no query-decomposition step. Fix: app-side split into sub-queries, retrieve each (one table_row lookup, one safety-callout retrieval), then merge with separate citations. |
| 12 | How do I reset my company laptop password? | ❌ | Retrieval always returns top-k with a score; nothing in the index flags 'not in corpus,' and docs explicitly place abstention in the chatbot app, not the index. Fix: app-side relevance-threshold + summary-scope check to abstain; optionally use record_type='summary' coverage to justify the refusal. |
| 13 | List the chapters in the overhead construction manual. | 🟡 | chapter_label/chapter_number/header_1 are facetable and can be faceted with source_file filter, but facets are unordered, string-sorted, and give no page ranges. Fix: a per-document structure record (ordered by chapter_number/layout_ordinal with page spans), or app-side sort of the chapter_label facet by layout_ordinal. |
| 14 | Show me the table of contents for document 18. | ❌ | TOC/index lines are classified is_locator_artifact=true with retrieval_eligible=false and are suppressed, so the literal 'X ...... 18-3' lines that answer this are excluded. Fix: keep TOC lines as a retrievable record_subtype='toc' (or synthesize a structure record) rather than dropping them as artifacts. |
| 15 | Which manual should I look in for live-line MAD? | 🟡 | record_type='summary' (one per PDF) plus operationalarea/functionalarea/doctype facets support routing, but 'MAD' is unexpanded and summaries may not name the acronym. Fix: synonym expansion + route on summary + facets. |
| 16 | What's the document number for the URD standard? | 🟡 | document_number and document_title are searchable, but 'URD standard' relies on the acronym matching the title text; without synonym expansion the title match is fragile. Fix: synonymMap URD→underground residential distribution; search document_title. |
| 17 | What's the latest revision of the grounding manual? | ✅ |  |
| 18 | Take me to the figure referenced in step 3 of the recloser procedure. | 🟡 | procedure_id/procedure_step_order plus figures_referenced_normalized and figure_step_linked/figure_linkage_confidence support the hop, but it is multi-step and linkage confidence varies. Fix: at query time, retrieve the step by procedure_id+order, then expand figures_referenced_normalized (per §4.4); rely on figure_step_linked to prefer the correct figure. |
| 19 | What's the table mentioned on page 18-5? | 🟡 | printed_page_label is filterable and tables_referenced/tables_referenced_normalized exist, but only if the page-18-5 chunk is retrieved first; there is no direct 'give me the table object referenced from page X' join. Fix: two-hop — filter printed_page_label='18-5', read tables_referenced_normalized, then fetch that table record. |
| 20 | What chapter number is 'Protective Grounding'? | 🟡 | chapter_number is filterable/facetable but populated on text records; answer depends on retrieving the right heading chunk. Fix: structure record mapping heading→chapter_number, or facet chapter_number under a header_1 filter. |
| 21 | Where in the manual is arc flash discussed? | 🟡 | section_path, header_1/2/3, printed_page_label give good location citations when relevant chunks rank, and topic_id groups a section across record types; but there is no aggregated 'all locations' rollup, so top-k may miss scattered mentions. Fix: raise top / facet on section_path; use topic_id to gather the section. |
| 22 | kcmil to mm2 conversion table | 🟡 | table/table_row records can hold it, but 'kcmil'/'mm2' are rare tokens and there is no synonym for conductor-size units. Fix: ensure conversion tables are row-split with semantic keys; add unit synonyms. |
| 23 | CT/PT wiring diagram | 🟡 | diagram_category='schematic_wiring' and has_diagram support the diagram route, but 'CT'/'PT' are unexpanded initialisms (also collide with 'PT' = potential transformer vs part). Fix: synonymMap CT→current transformer, PT→potential transformer; filter diagram_category. |
| 24 | LOTO steps | 🟡 | procedure_id/procedure_step_order/procedure_step_text give ordered steps, but 'LOTO' is not expanded to 'lockout/tagout'. Fix: synonymMap LOTO→lockout tagout; then procedure retrieval works. |
| 25 | NESC clearance requirements | 🟡 | 'NESC' is a distinctive token so BM25 helps, but the answer is usually a table lookup and NESC references may live in footnotes/callouts. Fix: ensure clearance tables row-split; search footnotes; optional synonym NESC→National Electrical Safety Code. |
| 26 | SF6 breaker maintenance interval | 🟡 | applies_to_equipment/equipment_ids can scope it and table_row can hold the interval, but 'SF6' tokenization (SF6 vs SF-6 vs sulfur hexafluoride) is inconsistent. Fix: normalize/synonym SF6 variants; row-split the interval table. |
| 27 | OCB oil sampling frequency | 🟡 | 'OCB' unexpanded; value likely a table_row. Fix: synonymMap OCB→oil circuit breaker; ensure the sampling schedule is a lookup-friendly table_row. |
| 28 | Show me the wiring for a 25 kVA pole xfmr | 🟡 | has_diagram/diagram_category and applies_to_equipment support it, but 'xfmr' unexpanded and '25 kVA' rating scoping depends on equipment_ids extraction quality. Fix: synonym xfmr→transformer; verify kVA ratings land in equipment_ids/applies_to_equipment. |
| 29 | Is confined-space entry even covered in these manuals? | 🟡 | Retrieval always returns something with a score; there is no 'topic absent' signal, and summaries only weakly enumerate scope. Fix: app-side coverage check (score threshold + scan record_type='summary' scope) to answer presence/absence honestly. |
| 30 | grounding | 🟡 | Semantic reranker helps but a bare topic word cannot distinguish procedure vs table vs diagram vs definition intent, so results are mixed. Fix: app-side clarification or default multi-type retrieval (search.in record_type) with a routing prompt. |
| 31 | page 42 | 🟡 | physical_pdf_page and printed_page_label are both filterable and page_resolution_method explains the mapping, but the query lacks a document context and 'page 42' is ambiguous between printed label and PDF index. Fix: require/carry source_file context; disambiguate printed_page_label vs physical_pdf_page and surface both. |
| 32 | Which physical PDF page is printed page '18-3'? | ✅ |  |
| 33 | What's on the next page after the MAD table? | 🟡 | chunk_next_id/chunk_prev_id are empty stubs, so true next-chunk following is broken; only layout_ordinal + physical_pdf_page ordering within a parent is available as a fallback. Fix: populate chunk_prev_id/chunk_next_id, or order by layout_ordinal/physical_pdf_page within parent_id at query time. |
| 34 | Show me the rest of that procedure. | 🟡 | Within a procedure_id, procedure_step_order/procedure_step_count let you fetch all steps; but cross-chunk continuation relies on chunk_next_id, which is empty. Fix: use procedure_id+step_order to pull the full step set; populate chunk_next_id for prose continuation. |
| 35 | Which section comes right before 18.4? | 🟡 | section_path and layout_ordinal allow inferring order, but there is no explicit previous-section pointer and section adjacency across record types isn't materialized. Fix: order by layout_ordinal within document, or add prev/next section pointers to a structure record. |
| 36 | Is there an abbreviations / acronym list in the manual? | 🟡 | record_subtype='glossary' can flag definition pages, but there is no dedicated acronym-list record or field, and glossary detection coverage is uncertain. Fix: ensure abbreviation/acronym pages are tagged record_subtype='glossary' and retrievable; expose an acronym field. |
| 37 | wut is d MAD | ❌ | No speller, no synonym/acronym map, and BM25 tokens ('wut','d') are noise; ada-002 cannot recover this. Fix: query normalization + speller + acronym map before retrieval. |
| 38 | What's the clearance for 12kv (also written 12 kV or 12,000 volts)? | 🟡 | applies_to_voltage exists as a facet but query-side voltage strings vary (12kv/12 kV/12000V) and there is no normalization synonym, so filter matches are brittle. Fix: normalize voltage tokens at query time and/or synonymMap for voltage formats aligned to applies_to_voltage values. |
| 39 | What changed between Rev B and Rev C of the grounding procedure? | 🟡 | document_revision/supersedes_revision/is_current_revision identify revisions, but a default is_current_revision filter hides the superseded one and there is no stored diff. Fix: retrieve both revisions by document_family_id, drop the current-only filter for compare intent, diff app-side. |
| 40 | Which is newer, the 2019 or 2021 URD standard? | 🟡 | effective_date is sortable/facetable so recency ordering works, but 'URD' is unexpanded so matching the right documents is fragile. Fix: synonym URD; then orderby effective_date desc. |
| 41 | Find the schematic on the same page as the neutral-bonding text. | 🟡 | parent_id, physical_pdf_page and record_type='diagram'+has_diagram support the §4.4 co-location pattern, but it requires first retrieving the neutral-bonding chunk then filtering by its page/parent. Fix: two-hop retrieval keyed on shared physical_pdf_page/parent_id. |
| 42 | Show me everything about reclosers. | 🟡 | topic_id (new cross-type section group) plus facets enable gathering text/diagram/table for a section, but breadth is limited by top-k and topic_id only groups within a section, not across the whole document. Fix: expand by topic_id after the first hit; raise top; facet record_type to ensure all types surface. |
| 43 | What's the title of chapter 18? | 🟡 | Filter chapter_number eq '18' then read header_1 works when a chapter-18 heading chunk exists, but chapter_number is string-typed (sorting/'18' vs '18.0' variance) and lives on text records only. Fix: normalize chapter_number; add a structure record with chapter_number→title. |
| 44 | Fig 18-117 / Figure 18.117 / figure18117 — same figure? | 🟡 | figures_referenced_normalized and figure_number provide a normalized index-side form, but the user's typed variant must be normalized the same way before filtering; there is no documented query-time normalizer. Fix: apply the same figure-ref normalization to the query token before the filter. |
| 45 | help | ❌ | No index signal distinguishes a meta/help utterance from a content query; retrieval will still return safety chunks. Fix: app-side intent gate to short-circuit to a help/onboarding response before retrieval. |
| 46 | Define arc-flash boundary and give the value for a 12kV switch. | 🟡 | The definition (glossary/text) and the value (table_row + applies_to_voltage) both exist but the compound single-embedding query under-retrieves one half. Fix: decompose into a definition sub-query (record_subtype='glossary') and a value sub-query (record_type='table_row'), cite both. |
| 47 | Which manuals cover gas and which cover electric? | 🟡 | applies_to_domain and operationalarea/functionalarea/doctype facets plus per-doc summaries support domain routing, but applies_to_domain is per-record and may be sparse on some chunks, so a document-level rollup isn't guaranteed. Fix: facet applies_to_domain/operationalarea scoped by source_file, or add a document-level domain on the summary record. |